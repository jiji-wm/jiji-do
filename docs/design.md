# jiji-do — Stage 1 design (skeleton + capability detection)

**Date:** 2026-05-27
**Status:** approved 2026-05-27 (design + written spec). This file is the **owning DD**, derived from the approved spec at `../../docs/superpowers/specs/2026-05-27-jiji-do-stage1-design.md` (which stays as the historical artifact). §1–11 below are the approved design verbatim; §12 has been rewritten for the unified jiji loop, and an `## Implementation phases` section has been appended as the loop-drivable ledger.
**Initiative:** `../../docs/launcher/initiative.md` (this is Stage 1; the launcher's end-state, capability model, dependency contract, and CLI surface live there)
**Name:** `jiji-do` — ratified 2026-05-27 (the §10 working-name decision; `jiji-cmd` / `jiji-palette` rejected)

## 1. Purpose

`jiji-do` is a Helix-style command launcher for the jiji desktop. One keybind opens a fuzzel menu of every actionable verb; each verb is also a top-level subcommand so direct keybindings skip the menu. It captures focused window/workspace/activity *once* at process start — before any picker grabs keyboard focus — and dispatches the snapshotted ids. It runs on upstream niri with a reduced verb set and fully on the jiji fork + `jiji-activities`.

**Stage 1 goal:** prove the wiring end-to-end with two pilot verbs and the capability-gating machinery. No verb-coverage breadth (that is Stage 2). This spec is the design for that first slice.

## 2. Scope

### In scope (Stage 1)
- Repo skeleton: `Cargo.toml`, `LICENSE`, `CLAUDE.md`, `docs/design.md` (the eventual home of this design), `src/main.rs`, `src/cli.rs`, `tests/`.
- Capability prober → a `Capabilities` bitflags value.
- `Snapshot` type built once at startup.
- Verb registry: a small struct per verb (label, category, required capabilities, dispatch fn).
- Two pilot verbs: `switch-workspace` (native) and `switch-activity` (passthrough, gated).
- Menu entry point (`jiji-do` no-arg) listing **enabled** verbs only; per-verb subcommand dispatch.
- `--debug` listing which verbs were filtered and why.
- Integration tests under `assert_cmd` + a sentinel-picker shim.

### Out of scope (Stage 1 — deferred)
- Verb-category menu ordering (**deferred to Stage 2** — only two verbs exist now; ordering is meaningless at n=2). Stage 1 lists verbs in registration order.
- The broad curated verb set (~20–30 verbs) — Stage 2.
- `jiji-activities` passthrough breadth — Stage 3.
- README / fish completions / packaging polish — Stage 4.
- Any `niri-ipc` Cargo dependency, daemon mode, D-Bus, plugin manifests, chord input, picker theming — Stage 5+ parking lot per the initiative.

## 3. §10 ratification decisions (resolved here)

1. **Final name:** `jiji-do`.
2. **Verb-category menu ordering:** deferred to Stage 2 (registration order for Stage 1).
3. **Snapshot caching across menu→verb dispatch:** the snapshot is read **once** at process start and passed by reference into the verb dispatch fn. Dispatch never re-reads compositor state. This is the core value of snapshot-at-launch: a picker stealing focus mid-flow cannot invalidate the captured ids.
4. **Per-verb capability degradation messaging:** invoking a capability-gated verb directly in an unsupported environment (e.g. `jiji-do switch-activity` on upstream niri, or with `jiji-activities` absent) prints a one-line stderr diagnostic naming the missing capability and exits **69**. The menu path simply hides disabled verbs (they never appear, so never error). `--debug` is the diagnostic surface for *why* a verb was hidden.

## 4. Architecture

Four units, each with one purpose, a defined interface, and independent testability.

### 4.1 Capability prober (`src/capabilities.rs`)
- Produces a `Capabilities` bitflags value. Flags: `NIRI_SOCKET`, `FUZZEL`, `FORK`, `NIRI_ACTIVITIES`. (`ROFI` reserved for a future multi-select verb; not probed in Stage 1 since no verb needs it.)
- Probes (each independent, no dispatch side effects):
  - `NIRI_SOCKET` — `$NIRI_SOCKET` set **and** a connect succeeds.
  - `FUZZEL` — `fuzzel` resolves on `$PATH` (same `$PATH`-walk discipline as jiji-activities' picker availability check).
  - `FORK` — `niri msg --json activities` returns the activities-shaped response (upstream niri lacks the subcommand / returns an error or an unrecognized shape).
  - `NIRI_ACTIVITIES` — `jiji-activities` resolves on `$PATH` **and** `jiji-activities --version` exits 0.
- Interface: `fn probe() -> Capabilities`. Depends on: env, `$PATH`, one `niri msg` subprocess, one `which`-style lookup.

### 4.2 Snapshot (`src/snapshot.rs`)
- `struct Snapshot { focused_window: Option<u64>, focused_workspace: Option<u64>, focused_output: Option<String>, focused_activity: Option<String> }`.
- Built once via `niri msg --json windows`, `... workspaces`, and (only if `FORK`) `... activities`, parsed into **minimal local serde structs** (only the fields read — not a re-export of niri-ipc types). This duplication is intentional and is the dependency-contract price (§4.5).
- Interface: `fn capture(caps: Capabilities) -> Result<Snapshot>`. The activity field is `None` on upstream. Each focused-* field is `None` when nothing is focused.

### 4.3 Verb registry (`src/registry.rs` + `src/verbs/`)
- A verb is a value: `struct Verb { name: &'static str, label: &'static str, category: Category, requires: Capabilities, dispatch: fn(&Snapshot) -> Result<()> }`.
- `Category` enum (Workspace / Activity / … ) exists from Stage 1 but is only used for grouping in Stage 2; Stage 1 ignores it for ordering.
- The registry is a static list of `Verb`s. `enabled(caps)` filters to verbs whose `requires` is satisfied by `caps`.
- A verb dispatches **only** if `caps.contains(verb.requires)` — enforced centrally at the dispatch boundary, not per-verb. Tests assert no verb can dispatch with unmet capabilities.

### 4.4 Dispatch + menu (`src/main.rs`, `src/cli.rs`, `src/menu.rs`)

**Snapshot is captured first, before any picker (including the menu's own fuzzel) opens.** Non-negotiable: opening fuzzel steals keyboard focus and changes what "focused" means, so a snapshot taken after the menu would capture fuzzel's state, not the user's. Order in both paths: probe capabilities → capture snapshot → then menu-or-direct-dispatch.

- `jiji-do` (no arg): requires `FUZZEL`; captures the snapshot, then renders a fuzzel menu of `enabled(caps)` verbs (label text); on selection, calls the verb's dispatch fn **with the already-captured snapshot**. (Menu does **not** re-exec `jiji-do <verb>` — it calls the same dispatch path inline, so both entry points share one test fixture and avoid double-process cost.)
- `jiji-do <verb>`: parses to the verb; if `caps` unmet → stderr diagnostic + exit 69 (§3.4); else capture snapshot, dispatch.
- `--debug`: prints the resolved `Capabilities` and, per verb, `kept` or `filtered (missing: X)`. Stays out of stderr on normal runs.

## 5. Pilot verbs

- **`switch-workspace`** (native; `requires = NIRI_SOCKET | FUZZEL`): fuzzel over the workspace list (from the snapshot / a `niri msg --json workspaces` read), then `niri msg action focus-workspace <ref>`.
- **`switch-activity`** (passthrough; `requires = NIRI_SOCKET | FUZZEL | FORK | NIRI_ACTIVITIES`): shells out to `jiji-activities switch` (which runs its own picker). Hidden on upstream / when `jiji-activities` is missing.

These two were chosen because they exercise both verb kinds (native dispatch via `niri msg`; passthrough via `jiji-activities`) and the full capability-gate spread (`switch-activity` requires all four flags; `switch-workspace` only two).

## 6. CLI surface

```sh
jiji-do                  # fuzzel menu of enabled verbs
jiji-do switch-workspace # direct dispatch (native)
jiji-do switch-activity  # direct dispatch (passthrough, gated)
jiji-do --debug          # capability + filter report
```

clap-derive, mirroring jiji-activities' structure (clap + anyhow). Per-verb subcommands are generated from the registry where practical, or kept in sync with it (the registry is the single source of truth for which verbs exist).

## 7. Capability model & exit codes

| Condition | Exit | Behavior |
|---|---|---|
| `$NIRI_SOCKET` unset / connect fails | 69 | stderr `"niri socket unavailable: …"`; nothing works without it. |
| `fuzzel` missing, picker verb or menu invoked | 69 | stderr `"picker unavailable: fuzzel not on $PATH"`. Immediate-dispatch verbs (none in Stage 1) would still work. |
| gated verb invoked directly, capability unmet | 69 | stderr names the missing capability (e.g. `"switch-activity requires the jiji fork + jiji-activities"`). |
| normal dispatch | 0 | — |

The binary **never errors on missing deps in menu mode** — it filters the verb out. Direct invocation of an unavailable verb is the only path that exits 69 on a capability miss.

## 8. Dependency contract (load-bearing — pin in `jiji-do` CLAUDE.md)

1. **No `niri-ipc` Cargo dep.** All compositor interaction is via `niri msg` subprocesses.
2. **No `jiji-activities` Cargo dep.** Activities verbs are subprocess passthroughs.
3. **All snapshots are JSON over `niri msg --json …`**, parsed with `serde_json` against minimal local structs (only the read fields).

These three are what let one binary run on both upstream niri and the jiji fork. A lint/grep test should assert `Cargo.toml` gains no `niri-ipc` dep.

## 9. Testing

- `assert_cmd` integration tests + a sentinel-picker shim (a tempdir `#!/bin/sh` script named `fuzzel`, env-clear + scoped `$PATH`), the same pattern jiji-activities uses.
- Cover: (a) both pilot verbs dispatch correctly under a sentinel picker; (b) the menu lists only enabled verbs for a given capability set; (c) a gated verb invoked directly with unmet capabilities exits 69 with the right stderr; (d) `--debug` reports kept/filtered correctly; (e) capability-gate enforcement — no verb dispatches with unmet capabilities.
- Capability probing is faked in tests by injecting a `Capabilities` value rather than touching the real environment (keeps tests hermetic; avoids the env-var-mutex isolation hazard seen in jiji-activities' `ipc::tests`).

## 10. Naming note (pre-compositor-rename)

This spec uses `niri msg` / `$NIRI_SOCKET` per the initiative's §11 glossary. The compositor exports both `$JIJI_SOCKET` and `$NIRI_SOCKET` today, so `niri msg` works. These become `jiji msg` / `$JIJI_SOCKET` only when the compositor source-rename sub-phase lands (deferred, Phase D-adjacent). `jiji-do` itself takes the `jiji-` name from the start (greenfield, no rename debt).

## 11. Exit criteria (Stage 1)

- Repo skeleton in place; builds.
- Capability prober, `Snapshot`, registry, both pilot verbs, menu, `--debug` all implemented.
- Integration test suite (§9) green.
- `Cargo.toml` carries no `niri-ipc` / `jiji-activities` dep (contract test passes).
- One round of loop review per the unified jiji loop (`/jiji:land-subphase jiji-do`).

## 12. Process follow-ups (rewritten for the unified jiji loop)

The launcher initiative originally anticipated a dedicated `jiji-do` sub-agent loop. The 2026-05-28 loop unification (`../../docs/superpowers/plans/2026-05-28-jiji-loop-unification.md`) replaced the per-project loops with one role-based agent set that routes by language via `loops.conf`, so bootstrapping jiji-do now costs **one registry row and zero new agents** (it is Rust → `jiji-rust-implementer`):

- This design moved to `repos/jiji-do/docs/design.md` as the owning DD, carrying a Phase 1.0 design-ratification gate (mirrors the jiji-activities ratification pattern) — see `## Implementation phases` below.
- ~~Bootstrap a `jiji-do` sub-agent loop (`.claude/agents/jiji-do-{architect,implementer,fixer,scribe}.md` + `.claude/commands/jiji-do/`).~~ **Obsolete under the unified loop.** Replaced by a single workspace `loops.conf` row: `jiji-do|rust|repos/jiji-do|repos/jiji-do/docs/design.md|repos/jiji-do`. The scope-discipline additions the initiative flagged (curate-don't-enumerate, capability-gate completeness, no-`niri-ipc`-link) live as per-codebase discipline in this repo's `CLAUDE.md`, read by `jiji-rust-implementer`.

---

## Implementation phases

The loop-drivable ledger. `/jiji:land-subphase jiji-do` (architect → implementer → review → fixer → scribe) lands these boxes; the architect scans from the topmost unchecked `[ ]` and groups consecutive qualifying boxes into a landing unit.

**Step recipes** for each implementation phase below live in `../../docs/superpowers/plans/2026-05-27-jiji-do-stage1.md` — **Phase 1.N corresponds to that plan's Task N** (file layout, exact code blocks, commit shape). That plan's Task 12 (per-loop bootstrap) is obsolete under the unified loop and is dropped.

### Phase 1.0 — Design ratification

- [x] **§3 decisions ratified** (name `jiji-do`; ordering deferred to Stage 2; snapshot-once semantics; capability-miss = exit 69). Ratified 2026-05-27 in the approved spec; re-affirmed in this owning DD. *(Pre-checked — design was approved pre-loop; no in-loop ratification needed.)*

### Phase 1.1 — Repo skeleton (plan Task 1)

- [x] `Cargo.toml` (no `niri-ipc`), `LICENSE`, `CLAUDE.md` (dependency contract + implementer discipline), `docs/design.md` (this DD), `src/main.rs` placeholder, `.gitignore`. *(Landed in the 2026-05-28 bootstrap.)*

### Phase 1.2 — Subprocess helpers (plan Task 2)

- [x] `src/proc.rs`: `run_capture(cmd, args)` and `which(bin)` thin subprocess helpers. Landed in `13c2f31`.

### Phase 1.3 — Capabilities (plan Task 3)

- [x] `src/capabilities.rs`: `Capabilities` bitflags + `probe()` (the four Stage 1 flags per §4.1). Landed in `4b425b9`.

### Phase 1.4 — Snapshot (plan Task 4)

- [x] `src/snapshot.rs`: `Snapshot` + minimal serde briefs + pure `from_json(...)` + `capture(caps)` (per §4.2; minimal local structs, no `niri-ipc`). Landed in `6b122da`.

### Phase 1.5 — Verb registry (plan Task 5)

- [x] `src/registry.rs`: `Category`, `Verb`, the static verb list, `enabled(caps)` (per §4.3; central capability-gate enforcement). Landed in `10d5f01`.

### Phase 1.6 — `niri` action helper + `switch-workspace` (plan Task 6)

- [x] `src/niri.rs` action dispatch + `src/verbs/switch_workspace.rs` native verb (fuzzel over workspaces → `niri msg action focus-workspace`). Landed in `b17cd23`.

### Phase 1.7 — `switch-activity` passthrough (plan Task 7)

- [x] `src/verbs/switch_activity.rs`: passthrough verb shelling to `jiji-activities switch`, gated on all four capability flags. Landed in `411c634`.

### Phase 1.8 — Error / exit-code model (plan Task 8)

- [x] `src/error.rs`: `DoError` enum + `exit_code()` (69 for capability misses; per §7). Landed in `5b1d546`.

### Phase 1.9 — CLI + dispatch + menu wiring + `--debug` (plan Task 9)

- [x] `src/cli.rs` (clap `Cli`/`Cmd`), `src/menu.rs` (`pick_one`), `src/main.rs` (probe → snapshot → menu-or-dispatch → exit-code map), `--debug` report (per §4.4). Landed in `308f286`.

### Phase 1.10 — Contract test: no `niri-ipc` dependency (plan Task 10)

- [x] `tests/cli.rs::no_forbidden_dependencies`: grep `Cargo.toml` asserts no `niri-ipc` / `jiji-activities` dep (enforces §8). Landed in `9208348`. *(Test renamed from `no_niri_ipc_dependency` — now also asserts no `jiji-activities` dep.)*

### Phase 1.11 — End-to-end shim tests (plan Task 11)

- [x] `tests/shims.rs`: `$PATH`-scoped shim harness exercising the menu + both verbs + capability filtering + exit-69 path (per §9). Landed in `d655290`; follow-up `a0eaccc` fixes the fuzzel cancel-vs-failure silent-failure and adds a discriminating regression test.

**Reviewed:** 2026-05-28 (`13c2f31`, `4b425b9`, `6b122da`, `10d5f01`, `b17cd23`, `411c634`, `5b1d546`, `308f286`, `9208348`, `d655290`, `a0eaccc`). Phases 1.2–1.11 in ten feature commits plus one review follow-up, baseline 0 → 23 tests (14 unit + 3 cli + 6 shims). Reviewed across code quality, silent-failure surface, test coverage, and dependency-contract enforcement. **Finding worth surfacing (fuzzel cancel-vs-failure silent-failure, `src/menu.rs`):** the initial `pick_one` implementation mapped every non-success exit from fuzzel to a clean `Ok(None)` cancel — signal kills, Wayland display errors, and other genuine failures were silently swallowed as user-cancel. Fixed in `a0eaccc` by discriminating fuzzel exit-1 (conventional cancel) from all other non-zero or signal-terminated exits; the latter now bail with the exit code and captured stderr. A shim test pins the contract: fuzzel exit-1 during `switch-workspace` → `jiji-do` exits 0, no `focus-workspace` dispatched. Reusable correctness lesson: subprocess launchers must discriminate the picker's conventional cancel code from other failures rather than collapsing all non-success. **Finding worth surfacing (argv-shim accounting for probe invocations):** the capability probe runs `jiji-activities --version` before verb dispatch; argv-recording shim tests must account for this invocation when asserting the complete argv record, or the probe call is silently untracked. The end-to-end shims in `tests/shims.rs` were authored with this in mind after the fixer surfaced the pattern. **DD also amended in this commit:** Phase 1.10 test name corrected from `no_niri_ipc_dependency` → `no_forbidden_dependencies` (also asserts no `jiji-activities` dep); five deferred findings recorded in new Appendix C. All §11 exit criteria satisfied; the §12 Task-12 obsolescence (per-loop agent scaffolding, superseded by the unified loop) was pre-folded at DD authoring and is void by design — no exit-criterion gap. Post-review fixes squashed into `a0eaccc`: `src/menu.rs` fuzzel-cancel discrimination + stderr capture + BrokenPipe attribution; discriminating shim regression test added. Same 23 tests green (14 unit + 3 cli + 6 shims); `cargo clippy --all --all-targets` zero warnings; `cargo +nightly fmt --all` clean. Stage 1 complete; proceed to Stage 2 (curated verb set + category-grouped menu ordering + jiji-activities passthrough breadth) with the reviewed base.

### Phase 2.0 — Stage 2 design ratification (human gate)

Stage 2 begins implementation only after a human ratifies the three design decisions below. The decisions are UX/curation calls that fall outside the architect's lane (`docs/launcher/initiative.md` §3 explicitly pins "curate, don't enumerate" as a deliberate UX call; §10 leaves verb-category ordering as an open question deferred from Stage 1 ratification). Each box below is a `**Proposed:**` gate — ratify in-place by flipping `[ ]` → `[x]`, optionally with an amendment note. Drafts below are the architect's recommended defaults; nothing is decided until a box is flipped.

- [x] **Proposed: Verb roster for Stage 2 (native compositor verbs, no fork dep).** The set below is the architect's draft, lifted from `docs/launcher/initiative.md` §4 Stage 2 with one cut. Ratify the list (or amend in-place: strike entries to drop them, add `+ <verb>` lines to extend, add a `(deferred to Stage 4)` note to park).

  **Amended 2026-05-28 (human ratification + curation-principle clarification).** A second curation principle is added on top of "curate, don't enumerate": **exclude verbs that are already on muscle-memory keybinds in the standard niri config.** Rationale: a launcher menu entry that duplicates a one-key shortcut is dead weight — the user reaches for the key, not the menu. The launcher's value lives in (a) discovery verbs without obvious keybinds, (b) picker-based verbs that need a fuzzel choice (already covered by Stage 1's `switch-workspace`/`switch-activity`), (c) infrequent/debug verbs, and (d) Stage 3 activities passthrough (no keybinds exist). Ground truth for "muscle-memory keybind" is `~/.local/share/chezmoi/dot_config/niri/config.kdl.tmpl` — survey done against that config at amendment time.

  Surviving roster after applying the principle (2 verbs):
  - **Workspace nav** (Category::Workspace):
    - `focus-workspace-previous` — immediate; `niri msg action focus-workspace-previous`. Requires `NIRI_SOCKET`. The previous-workspace toggle is **not** bound in the standard config (line 649 is commented out) and is the only nav verb where a menu entry adds value.
  - **Mode toggles** (Category::Mode — new):
    - `toggle-debug-tint` — immediate; `niri msg action toggle-debug-tint`. Requires `NIRI_SOCKET`. Debug/diagnostic verb, no keybind, infrequent use — classic discovery candidate.

  Cuts from initiative §4 Stage 2 — original draft (`focus-window`, `close-window-by-fuzzel`, `move-window-to-monitor`/`move-column-to-monitor`, `move-window-to-workspace`/`move-column-to-workspace`, `toggle-workspace-sticky`) plus the amendment cuts (`focus-workspace-up`, `focus-workspace-down`, `close-window`, `fullscreen-window`, `toggle-window-floating`, `center-focused-column`/`center-column`, `toggle-overview`, `toggle-keyboard-shortcuts-inhibit`). Rationale for amendment cuts: all bound to Mod-prefixed keys in the standard config — `focus-workspace-up/down` to `Mod+PgUp/Dn`, `Mod+K/J`, `Mod+WheelUp/Dn`; `close-window` to `Mod+Q`; `fullscreen-window` to `Mod+Shift+F`; `toggle-window-floating` to `Mod+Semicolon`; `center-column` to `Mod+C` (note: original draft used `center-focused-column` which is the wrong action name — that's a config setting, not an action; the bound action is `center-column`); `toggle-overview` to `Mod+O`; `toggle-keyboard-shortcuts-inhibit` to `Mod+Escape` (with `allow-inhibiting=false` so it works even when inhibit is active).

  **Outcome of ratifying this box (as amended):** Phase 2.1 lands the entire surviving roster as **one** coarse sub-phase — both verbs share the immediate-dispatch shape, registry shape, and shim-test shape; splitting them is over-fragmentation. Phase 2.1 also wires the `Category` variant additions (Workspace already exists; Mode is new) and drops the `#[allow(dead_code)]` on `Category`. The full set of cuts moves to a new Appendix D (deferred Stage 2 verbs) so future sessions don't re-litigate and a future amendment can restore from there with rationale. The launcher's center of gravity shifts to Stage 3 (jiji-activities passthrough), where keybind duplication isn't a concern.

- [x] **Proposed: Verb-category menu ordering policy.** `docs/launcher/initiative.md` §10 enumerates three options (alphabetical, frequency-sorted, category-grouped). The architect recommends **category-grouped, stable within category** for these reasons: (a) it gives the user a learnable spatial map (Workspace verbs always at top, Activity verbs always at the same relative position), (b) frequency-sorted requires state (a usage counter) and re-ranking, which adds storage + reordering logic the launcher otherwise has none of, (c) alphabetical sounds principled but mixes unlike verbs (`close-window` between `center-focused-column` and `focus-workspace-up` — no spatial intuition). Within a category, **registration order** stays the source of truth (it's already the Stage 1 convention; the registry is hand-curated, so the author can put the most-used verb first per category).

  Concrete ordering for the drafted roster (assuming the roster box above is ratified as drafted): **Workspace → Window → Mode → Activity**. Activity verbs (Stage 3) come last so the fork-only category sits at the bottom where users who are on upstream don't see a hole.

  **Outcome of ratifying this box:** Phase 2.1 (or a dedicated micro-sub-phase) wires `Category` into `enabled()` ordering, drops the `#[allow(dead_code)]` on `Category` in `src/registry.rs:24`, adds a unit test pinning the category order.

- [x] **Proposed: Stage 2 vs Stage 3 batching.** `docs/launcher/initiative.md` §4 keeps Stage 2 (native compositor verbs) and Stage 3 (`jiji-activities` passthrough verbs) as separate stages. The workspace CLAUDE.md Resume cue flattens them ("Stage 2 = curated verb set + category-grouped menu ordering + jiji-activities passthrough breadth"). Architect recommends **keeping them separate** for these reasons: (a) Stage 2 verbs are upstream-compatible and exercise the native dispatch path; Stage 3 verbs all require `FORK | NIRI_ACTIVITIES` and exercise the passthrough path — splitting reviews the two dispatch shapes independently, (b) Stage 3 passthrough verbs have already been spec'd at the initiative level (`switch-activity-previous`, `move-window-to-activity`, `move-window-here`, `move-workspace-to-activity`, `assign-workspace`, `create-activity`, `remove-activity`, `save-activity`, `list-activities` — see initiative §4 Stage 3) and need their own Phase 3.x boxes (mirroring Phase 2.x), not a smush into Stage 2, (c) the loop-iteration discipline (one landing unit ~= one PR's worth of cognitive surface) is well-served by the split.

  **Outcome of ratifying this box:** Phase 2.x is native verbs only (workspace nav + window lifecycle + mode toggles + category ordering). Phase 3.x is `jiji-activities` passthrough breadth, authored separately once Phase 2 lands. The workspace CLAUDE.md Resume cue gets a one-liner correction in the next scribe pass.

### Phase 2.1 — Stage 2 surviving roster + Category ordering (one coarse sub-phase)

Lands the entire ratified surviving roster (2 verbs) plus the Category-grouped menu ordering wiring as **one** landing unit, per the Phase 2.0 outcome ("Phase 2.1 lands the entire surviving roster as **one** coarse sub-phase"). Both verbs share the immediate-dispatch shape, so they fold together cleanly; ordering wiring rides along because the new `Category::Mode` variant has to land in the same commit to compile.

- [x] `src/verbs/focus_workspace_previous.rs` — immediate-dispatch native verb shelling to `niri msg action focus-workspace-previous`. Capability: `Capabilities::NIRI_SOCKET` only (no `FUZZEL` — immediate verbs work without a picker per initiative §5). Category: `Workspace`. Landed in `c1fd6d7`.
- [x] `src/verbs/toggle_debug_tint.rs` — immediate-dispatch native verb shelling to `niri msg action toggle-debug-tint`. Capability: `Capabilities::NIRI_SOCKET` only. Category: `Mode`. Landed in `c1fd6d7`.
- [x] `src/niri.rs` — add `pub fn run_action(name: &str) -> anyhow::Result<()>` for zero-arg actions (thin wrapper around `proc::run_capture("niri", &["msg", "action", name])`). Keep `focus_workspace(id)` as-is — it carries an argument. The two new verb modules dispatch through `run_action`. Landed in `c1fd6d7`.
- [x] `src/registry.rs` — add `Category::Window` and `Category::Mode` variants in declarative order `Workspace, Window, Mode, Activity` (Window listed even though no Stage 2 verb sits there — declarative future-proofing for Stage 3 / restore-from-Appendix-D). Derive `PartialOrd, Ord` on `Category` (declaration order = sort order). Drop the `#[allow(dead_code)]` on `Category` at line 23 (now read by the ordering sort). Register the two new verbs in the static `REGISTRY` array. Landed in `c1fd6d7`.
- [x] `src/registry.rs::enabled` — change from registration-order filter to category-grouped stable sort: filter by capability, then `sort_by_key(|v| v.category)`. `sort_by_key` is stable in Rust's std, so registration order survives within each category. Landed in `c1fd6d7`.
- [x] `src/registry.rs::tests` — add a unit test pinning category-grouped ordering against a mixed-category fixture (assert a `Workspace` verb sorts ahead of a `Mode` verb even if the latter is registered first in the static array; assert intra-category registration order is preserved). Landed in `c1fd6d7`.
- [x] `tests/shims.rs` — add one shim test per new verb asserting the recorded `niri msg action <name>` invocation lands (reuses the existing `niri_body` `*)` recording branch — `$3 $4` captures `<name>` for `toggle-debug-tint`, and the existing patterns echo for unmatched `--json` args). Extend `debug_reports_filtering_upstream` with two `.stdout(predicates::str::contains(...))` assertions confirming both new verbs are `kept` on upstream (they require only `NIRI_SOCKET`, so they work on upstream niri). Landed in `c1fd6d7`.
- [x] `CLAUDE.md` — fold the two curation principles into a new "Curation discipline" paragraph under "Implementer discipline": (1) curate-don't-enumerate, (2) exclude verbs on muscle-memory keybinds in `~/.local/share/chezmoi/dot_config/niri/config.kdl.tmpl` (ratified 2026-05-28). Editorial; lands in the same commit. Landed in `c1fd6d7`.

**Reviewed:** 2026-05-28 (`c1fd6d7`, was `748be45` pre-fixer, `dacf520` after first amend). Phase 2.1 — the entire ratified Stage 2 surviving roster (2 verbs) plus Category-grouped menu ordering — landed as one coarse commit per the Phase 2.0 outcome. Reviewed across code quality, silent-failure surface, comment accuracy, test coverage, and type design. **Finding worth surfacing (CLAUDE.md banned-reference cleanup, `CLAUDE.md`):** the new "Curation discipline" sub-section in the initial commit referenced `(initiative §4)`, `Phase 2.0 ratification`, and `Appendix D` — all tokens on the hook's banned-reference list. Rephrased in the final commit to drop the parenthetical, replace the ratification reference with a datestamp-only note (2026-05-28), and refer to "the cut-verb candidates section in `docs/design.md`" without the Appendix-letter naming. **Finding worth surfacing (sort stability test overpromise, `src/registry.rs::tests`):** the initial test `category_grouped_ordering_pins_workspace_before_mode_regardless_of_registration_order` pinned the live REGISTRY output but did not vary registration order, so `sort_unstable_by_key` would have passed it — the test name overclaimed. Added `sort_by_key_preserves_intra_category_registration_order` with four locally-constructed same-category `Verb`s in reverse registration order. Subsequent re-review tightened the rustdoc: even 4 same-category elements is not a true stable-vs-unstable discriminator because pdqsort's insertion-sort fallback handles slices ≤20 elements stably in practice; the rustdoc now describes what the test actually pins (a behavioral order-preservation invariant) rather than the technically-stronger stable-sort claim. **Finding worth surfacing (action-failure shim contract, `tests/shims.rs::niri_action_failure_propagates_nonzero`):** added a shim test pinning that a failing `niri msg action` exits jiji-do with non-zero, not 69 (which is reserved for capability-miss). Historical analogue: the fuzzel cancel-vs-failure pattern in `a0eaccc` — both record the lesson that process-launcher code must discriminate error classes rather than collapsing all non-success. **Finding worth surfacing (`run_action` error contract rustdoc, `src/niri.rs::run_action`):** extended the rustdoc to name the `Err` conditions explicitly (non-zero exit or `niri` missing on `$PATH`), so callers know exactly what a returned `Err` implies without reading the implementation. **Finding worth surfacing (`Window` variant comment rot, `src/registry.rs`):** the initial comment `// no Window verbs yet` is temporal and will silently become false. Replaced with a structural description: `` `#[allow(dead_code)]` suppresses the unused-variant lint until the first Window-category verb is registered. `` This pins the rationale, not the current state, and survives future verb additions without rotting. Post-review fixes squashed into `c1fd6d7` (via `748be45` → `dacf520` fixer pass, then `c1fd6d7` rustdoc correction): CLAUDE.md banned-reference cleanup; `sort_by_key_preserves_intra_category_registration_order` stability test added with rustdoc tightened; `niri_action_failure_propagates_nonzero` shim test added; `run_action` rustdoc `Err` conditions named; `Window` variant comment rephrased to structural. Test count 23 → 28 (14 unit + 3 cli + 6 shims → 16 unit + 3 cli + 9 shims); `cargo +nightly fmt --all` clean; `cargo clippy --all --all-targets` zero warnings. Stage 2 surviving roster complete; the Phase 2.1+ ledger and Phase 3.x (jiji-activities passthrough breadth) boxes are deferred — the next architect pass authors Stage 3 per the center-of-gravity shift established at Phase 2.0 ratification. Proceed to Stage 3 authoring with the reviewed base.

---

> Phases 2.2+ (if any) — no further Stage 2 native verbs are queued after the 2026-05-28 ratification cut. Restoring any of the Appendix D candidates is an explicit human call.

### Phase 3.1 — Stage 3 passthrough core: snapshot-consuming verbs (one coarse sub-phase)

Lands the five `jiji-activities` passthrough verbs whose shape is "dispatch with the launch-time snapshotted id" — `switch-activity-previous` (no snapshot), `move-window-to-activity`, `move-window-here`, `move-workspace-to-activity`, and `assign-workspace`. All five share the passthrough dispatch shape (shell to `jiji-activities <verb>` with `--window`/`--workspace` filled in from the snapshot where applicable); all gate on the activities-capability cluster; all rely on Stage 0's explicit-id flags landed at `f948599` / `2a304a2`. The remaining initiative §4 Stage 3 verbs (`create-activity`, `remove-activity`, `save-activity`, `list-activities`) are deferred to a follow-up sub-phase — they carry undecided design questions (freeform name input via picker, stdout-via-keybind destination) that fall outside the "snapshot id → passthrough" shape.

The five verbs ride together because (a) they share one verb-module shape that the new mini-helper centralizes, (b) capability requirements split cleanly into two sub-shapes only (no-fuzzel toggle vs. with-fuzzel picker passthrough), (c) the registry + shim-test surface is one wave (one new verb-module file per verb plus one or two shim tests per verb), and (d) bundling avoids five round-trips through architect/implementer/scribe for what is genuinely one cognitive unit. Matches the Phase 2.1 precedent for coarse Stage-equivalent landings.

**Snapshot-empty contract (pinned here).** When a snapshot-consuming verb (`move-window-to-activity`, `move-window-here` for `--window`; `move-workspace-to-activity`, `assign-workspace` for `--workspace`) launches with `snapshot.focused_window` / `focused_workspace` = `None`, the verb **bails with stderr `"no focused <window|workspace> at launch"` and exits non-zero (exit 1, NOT 69)**. Exit 69 is reserved for capability misses; "no focused window" is a runtime-data miss. The bail must happen before any `jiji-activities` subprocess fires — falling through to `jiji-activities` with no `--window` would let it re-read focused state from a compositor whose focus has been stolen by the launcher menu's fuzzel, breaking the snapshot-at-launch contract that is the launcher's core value. This mirrors the historical lessons from `a0eaccc` (fuzzel cancel-vs-failure) and Phase 2.1's `niri_action_failure_propagates_nonzero`: discriminate error classes, don't collapse them.

**Cross-loop status note (non-blocking).** The compositor's cross-activity `move-window` path is currently a silent no-op (Phase 2.18 queued in the compositor loop). `jiji-do move-window-to-activity` is correctly wired here but is non-functional in production until that compositor fix lands. Landing Phase 3.1 does not worsen the situation — the verb plumbs through to the same `jiji-activities move-window` that is already exposed, with the existing (broken-against-other-activity) compositor behavior. Surfaced for awareness, not as a gate.

- [x] `src/verbs/switch_activity_previous.rs` — passthrough verb shelling to `jiji-activities switch-previous`. Capability: `Capabilities::NIRI_SOCKET | Capabilities::FORK | Capabilities::NIRI_ACTIVITIES` (no `FUZZEL` — `switch-previous` is a pure toggle and runs no picker). Category: `Activity`. Takes no snapshot field. Body: `crate::proc::run_capture("jiji-activities", &["switch-previous"])?; Ok(())`. Landed in `4a95f3b`.
- [x] `src/verbs/move_window_to_activity.rs` — passthrough verb shelling to `jiji-activities move-window --window=<id>`. Capability: `Capabilities::NIRI_SOCKET | Capabilities::FUZZEL | Capabilities::FORK | Capabilities::NIRI_ACTIVITIES` (FUZZEL because `jiji-activities move-window` spawns its own activity picker when no positional name is supplied). Category: `Activity`. Reads `snapshot.focused_window`; bails per the snapshot-empty contract above if `None`. The launcher passes no positional name and lets `jiji-activities` run its own picker (the snapshot supplies only the window id, not the destination activity). Landed in `4a95f3b`.
- [x] `src/verbs/move_window_here.rs` — passthrough verb shelling to `jiji-activities move-window-here --window=<id>`. Capability: `Capabilities::NIRI_SOCKET | Capabilities::FUZZEL | Capabilities::FORK | Capabilities::NIRI_ACTIVITIES` (FUZZEL because `move-window-here` runs a workspace picker internally — it is unit-shaped with no name positional, picker-only by design). Category: `Activity`. Reads `snapshot.focused_window`; bails per the contract if `None`. Landed in `4a95f3b`.
- [x] `src/verbs/move_workspace_to_activity.rs` — passthrough verb shelling to `jiji-activities move-workspace --workspace=<id>`. Capability: same four-flag set as `move-window-to-activity`. Category: `Activity`. Reads `snapshot.focused_workspace`; bails if `None`. Landed in `4a95f3b`.
- [x] `src/verbs/assign_workspace.rs` — passthrough verb shelling to `jiji-activities assign-workspace --workspace=<id>`. Capability: same four-flag set (FUZZEL because `assign-workspace` is multi-select-rofi-internally — but `jiji-activities` itself probes for rofi when invoked; from jiji-do's side it just shells out and propagates exit codes). Category: `Activity`. Reads `snapshot.focused_workspace`; bails if `None`. Landed in `4a95f3b`.
- [x] `src/verbs/mod.rs` — register the five new modules (`pub mod switch_activity_previous; pub mod move_window_to_activity; pub mod move_window_here; pub mod move_workspace_to_activity; pub mod assign_workspace;`). Landed in `4a95f3b`.
- [x] `src/registry.rs` — add five `Verb` entries to `REGISTRY` after the existing `switch-activity` entry, in initiative-§4 order: `switch-activity-previous`, `move-window-to-activity`, `move-window-here`, `move-workspace-to-activity`, `assign-workspace`. All under `Category::Activity`. The category-grouped sort already lands them at the bottom of the menu; intra-category registration order preserves the listed order. Landed in `4a95f3b`.
- [x] `src/verbs/<each>.rs` — extract a shared `dispatch_with_window(snapshot, verb)` / `dispatch_with_workspace(snapshot, verb)` helper if the `move-*` and `assign-workspace` bodies converge (judgment call during implementation — implementer kept them inlined per-verb: each verb module is 8–15 lines with an explicit bail; the three-call-site threshold was not reached). The `snapshot.focused_window.ok_or_else(|| anyhow::anyhow!("no focused window at launch"))?` shape is the load-bearing part; landed inline in `4a95f3b`.
- [x] `tests/shims.rs` — one shim test per snapshot-consuming verb pinning the argv that `jiji-activities` receives. The shim records `"$@"`; assertions use `lines.contains(&"<expected>")` (per the capability-probe `--version` invocation pattern), with the per-verb expected argv being:
  - `switch-activity-previous` → `"switch-previous"`
  - `move-window-to-activity` → `"move-window --window=11"` (matches the existing `niri_body` fixture's `focused: true` window id 11)
  - `move-window-here` → `"move-window-here --window=11"`
  - `move-workspace-to-activity` → `"move-workspace --workspace=21"` (matches the workspace fixture id)
  - `assign-workspace` → `"assign-workspace --workspace=21"`
  Landed in `4a95f3b`.
- [x] `tests/shims.rs` — one negative test for the snapshot-empty contract, scoped to one representative verb (`move-window-to-activity`): set up a `niri_body`-style shim where the windows JSON has `is_focused: false` everywhere; invoke `jiji-do move-window-to-activity` against the full-capability environment; assert exit code is non-zero AND non-69 (use `predicates::ord::ne(69)` plus a `.failure()` assert), and assert no argv line was recorded for `jiji-activities` (the dispatch must bail before the subprocess). One representative test is enough — the contract is the same for the other three snapshot-consuming verbs; coverage breadth here would be duplicative. **Fixer-added** (`move_workspace_to_activity_bails_when_no_focused_workspace`) per review finding: a Shape B negative bail test for the workspace-snap path was missing from the original commit (`362e7c2`); the fixer squashed it in, making the amended final commit `4a95f3b`. Two negative bail tests total cover both snapshot-field shapes (window and workspace). Landed in `4a95f3b`.
- [x] `tests/shims.rs` — extend `debug_reports_filtering_upstream` to assert the five new verbs are `filtered` on upstream (they all require `FORK`, which is absent there). One `.stdout(predicates::str::contains("<verb>: filtered"))` line per verb. Landed in `4a95f3b`.
- [x] `src/registry.rs::tests` — extend `enabled_filters_by_capability` or add a new test pinning the four-flag activities cluster: with `NIRI_SOCKET | FUZZEL | FORK | NIRI_ACTIVITIES` set, all five new verbs (plus the existing `switch-activity`) appear in `enabled(caps)` output. The category-grouped ordering test already covers Activity-last; it does not need updating for verb count but may need its registry-order assertion extended to spell out the six Activity-category verbs in declaration order. Landed in `4a95f3b`.

**Reviewed:** 2026-05-28 (`4a95f3b`, was `362e7c2` before fixer amend). Phase 3.1 — five `jiji-activities` snapshot-consuming passthrough verbs (`switch-activity-previous`, `move-window-to-activity`, `move-window-here`, `move-workspace-to-activity`, `assign-workspace`) — landed as one coarse commit per the DD's "one cognitive unit" rationale. Reviewed across code quality, silent-failure surface, test coverage, and contract pinning. **Finding worth surfacing (Shape B negative bail test gap, `tests/shims.rs`):** the original commit (`362e7c2`) covered the window-snapshot-empty bail path (`move-window-to-activity` with no focused window, `move_window_to_activity_bails_when_no_focused_window`) but lacked an analogous test for the workspace-snapshot-empty bail path. The fixer added `move_workspace_to_activity_bails_when_no_focused_workspace` before amending to `4a95f3b`. The two tests together cover both snapshot-field shapes; the other two workspace-consuming verbs (`assign-workspace`) share the identical bail path and are covered by behavioral symmetry — one representative per shape is the established DD contract. **Finding worth surfacing (cross-cutting `proc::run_capture` cancel-vs-failure gap, pre-existing):** `proc::run_capture` maps subprocess exit-69 leakage and signal-kill collapse to -1 equivalently to other failure codes — this is a pre-existing concern equally present in the `switch-activity` verb from Phase 2.1, not introduced by Phase 3.1. Not gated here; surfaced for awareness. A future refactor pass (exit-code class discrimination analogous to `a0eaccc`'s fuzzel cancel-vs-failure fix) applies uniformly to all subprocess-dispatching verbs and should be tackled cross-cuttingly rather than per-verb. **Finding worth surfacing (inlined vs. helper judgment call, `src/verbs/`):** implementer kept all five verb modules inlined (8–15 lines each) per the spec's "may keep inlined if ≤3 call sites" clause. The `anyhow!("no focused window/workspace at launch")` bail shape appears four times across the modules; if a fifth verb of this shape is added in Phase 3.2+, the three-call-site threshold for extraction will be reached — that is the natural trigger for introducing `dispatch_with_window` / `dispatch_with_workspace` helpers. Post-review fixes squashed into `4a95f3b`: `move_workspace_to_activity_bails_when_no_focused_workspace` test added at `tests/shims.rs` (Shape B workspace-snap negative bail). Test count 28 → 36 (17 unit + 3 cli + 16 shims; +8 vs. baseline: +1 unit for the four-flag activities cluster test, +7 shims: 5 positive argv-pin + 1 window-bail + 1 workspace-bail; spec predicted +7, fixer's Shape B test is the extra +1). Same 36 tests green; `cargo clippy --all --all-targets` zero warnings. Proceed to Phase 3.2+ authoring (`create-activity`, `remove-activity`, `save-activity`, `list-activities`) with the reviewed base.

---

### Phase 3.2 — Stage 3 remaining verbs design ratification (human gate)

The four remaining `jiji-activities` passthrough verbs (`create-activity`, `remove-activity`, `save-activity`, `list-activities`) carry materially different UX shapes — unlike Phase 3.1's five snapshot-consuming verbs, these do **not** combine into one cognitive unit. Each needs its own architectural call before implementation can be authored. Each box below is a `**Proposed:**` gate — ratify in-place by flipping `[ ]` → `[x]`, optionally with an amendment note. Drafts below are the architect's recommended defaults; nothing is decided until a box is flipped.

**Cross-cutting decision baked in (not part of any individual ratification box):** the `Snapshot` struct stays as-is. It is contractually "focused state at launch" (one window id, one workspace id, one output name, one activity name). The full activities **inventory** that `remove-activity`'s picker needs is a different concept; reading it at verb-dispatch time via `niri msg --json activities` (one extra subprocess on a verb that runs once per user keypress) is cheaper than broadening `Snapshot` into a general-purpose IPC cache. Confirmed by the Phase 3.1 advisor pass; non-controversial.

- [x] **Proposed: `save-activity` shape — snapshot-consuming passthrough.** Identical shape to Phase 3.1's five verbs. Reads `snapshot.focused_activity` (already captured); bails with stderr `"no focused activity at launch"` and exits non-zero (NOT 69, per the Phase 3.1 snapshot-empty contract) if `None`. Shells to `jiji-activities save <name>` with the snapshotted activity name as positional. Capability: `NIRI_SOCKET | FORK | NIRI_ACTIVITIES` (no FUZZEL — no picker spawned by jiji-do; `jiji-activities save` itself is non-interactive). Category: `Activity`.

  **Rationale:** the snapshot already carries `focused_activity`; this verb is the natural use case. The user picks `Save activity` from the menu, the launcher saves *the current activity* (the only sensible interpretation — "save" with no target is ambiguous, and a fuzzel picker for "which activity?" defeats the launcher's snapshot-at-launch value when the natural target is the focused one). The bail-on-`None` follows the Phase 3.1 precedent for `move-window-to-activity` / `move-workspace-to-activity`.

  **Outcome of ratifying:** Phase 3.2a lands `src/verbs/save_activity.rs` (~12 lines, identical pattern to `move_window_to_activity.rs`), registers in `REGISTRY`, adds one positive shim test (`save Work` argv) and one negative bail test (`focused_activity: None` → non-zero, non-69, no argv dispatched).

- [x] **Proposed: `create-activity` shape — freeform-name fuzzel prompt + direct-CLI form.** Two entry points: (1) menu path — `jiji-do` opens its menu, user picks `Create activity`, the launcher spawns `fuzzel --dmenu` with empty stdin (free-text prompt; the established pattern lives in `repos/jiji-activities/src/picker/single_select.rs::prompt_name`), the user types a name and presses Enter, the launcher shells to `jiji-activities create <typed-name>`. Empty input / cancel → exit 0 (clean no-op, mirrors `pick_one` cancel semantics). (2) Direct-CLI path — `jiji-do create-activity <name>` from a keybind skips the prompt. Capability: `NIRI_SOCKET | FUZZEL | FORK | NIRI_ACTIVITIES`. Category: `Activity`. **Snapshot unused.**

  **New surface in jiji-do:** a `menu::prompt_name(prompt: &str) -> anyhow::Result<Option<String>>` helper (mirrors `picker::prompt_name` in jiji-activities — fuzzel `--dmenu` with empty stdin; success+non-empty → `Some(typed)`, success+empty / exit-1 cancel → `None`, other non-zero → `bail!` per the established cancel-vs-failure discipline from `a0eaccc`). Reusable for any future freeform-name verb. The `create-activity` verb body branches on the new optional positional arg.

  **CLI surface change:** the verb takes an optional positional name. The current `cli::Cli` shape is `Cli { debug: bool, verb: Option<String> }` — adding a name positional means either (a) reshape `verb` to a subcommand-derived enum (large diff, opens the door to per-verb args properly), or (b) add a second positional `Cli { debug: bool, verb: Option<String>, verb_arg: Option<String> }` (smaller diff, conservative; the dispatch fn signature for `create-activity` reads the second positional from a `&Cli` parameter or via a side channel). The architect recommends **(b)** for this batch — option (a) is a larger architectural reshape best deferred to a dedicated sub-phase if/when a second name-bearing verb arrives. Either is implementer's call inside the ratified shape; the spec will commit to one.

  **Rationale:** the launcher's value proposition is menu-driven discovery; "you can create an activity from your launcher" is a real workflow. Refusing the menu path and forcing direct CLI only would be the path of least resistance but would leave the verb dead weight in the menu (it would have to be hidden). The freeform-prompt is launcher-flavored and reuses an established pattern from the sibling repo.

  **Outcome of ratifying:** Phase 3.2b lands `src/menu.rs::prompt_name` (~30 lines, near-clone of jiji-activities' `prompt_name`), `src/verbs/create_activity.rs` (~25 lines: prompt if no positional, bail on empty, shell out), CLI surface change per (a) or (b), 2 shim tests (positive: prompt returns "X" → argv `create X`; cancel: empty stdout → exit 0, no argv).

- [x] **Proposed: `remove-activity` shape — picker over existing activity names.** Two entry points: (1) menu path — `jiji-do` opens its menu, user picks `Remove activity`, the launcher reads `niri msg --json activities` (one extra subprocess, not added to `Snapshot` per the cross-cutting decision above), feeds the activity names to `fuzzel pick_one` for selection, then shells to `jiji-activities remove <picked-name>`. Cancel → exit 0. (2) Direct-CLI path — `jiji-do remove-activity <name>` skips the picker (same CLI-surface shape as `create-activity` per that box's recommendation (b)). Capability: `NIRI_SOCKET | FUZZEL | FORK | NIRI_ACTIVITIES`. Category: `Activity`. **Snapshot unused.**

  **Rationale:** removing the currently-focused activity is the wrong default — the user just typed `remove` and intends to clean up an *other* activity (otherwise they'd type `switch` first). A picker over the activity inventory matches `jiji-activities`' own conventions for verbs that select from inventory rather than acting on the focused thing. The inventory is read fresh at dispatch (not snapshotted) because (a) `Snapshot` purity, (b) inventory changes are rare so a stale snapshot would rarely be wrong but always feels wrong, (c) one extra subprocess on a verb that runs at user-keypress cadence is invisible.

  **Outcome of ratifying:** Phase 3.2c lands `src/verbs/remove_activity.rs` (~30 lines: read activities JSON via `proc::run_capture`, minimal serde brief reusing `ActivityBrief` from `snapshot.rs` if pub-exposed, or a new inline brief; `pick_one` the names; shell to `jiji-activities remove <name>`), 2 shim tests (positive: niri activities shim → fuzzel echoes name → argv `remove <name>`; cancel: fuzzel exit-1 → exit 0, no argv). Implementer's judgment whether to lift `ActivityBrief` to `pub(crate)` or inline a duplicate.

- [x] **Proposed: `list-activities` shape — direct-CLI only, hidden from menu.** Registered as a verb (so it gets a subcommand) but flagged not-for-menu so it doesn't appear in the fuzzel list. Direct invocation `jiji-do list-activities` shells to `jiji-activities list` and prints its stdout verbatim. Capability: `NIRI_SOCKET | FORK | NIRI_ACTIVITIES` (no FUZZEL — no picker; also no menu visibility means FUZZEL would be irrelevant either way). Category: `Activity`.

  **Verb-shape change required:** `Verb` currently has no notion of "registered but hidden from menu." Add `menu_visible: bool` field (default `true`). `registry::enabled(caps)` continues to return all enabled verbs (so `--debug` still surfaces `list-activities`); the menu render path in `main.rs` filters additionally on `v.menu_visible` before passing to `menu::render_menu`. Direct dispatch (`registry::find` + `is_enabled`) is unchanged. The architect recommends `menu_visible: bool` over (a) a `Category::Hidden` sentinel (overloads `Category` with non-category semantics; breaks the category-grouped sort invariant by introducing a category that doesn't sort meaningfully) or (b) skipping the registry entirely and routing at `cli.rs` (creates a second dispatch path; breaks the "registry is the single source of truth" invariant pinned in §4.3).

  **Rationale:** the initiative §4 Stage 3 entry explicitly calls `list-activities` "a data verb; rarely menu material but exposed for completeness." A menu pick that prints to stdout is nonsense UX in a fuzzel-launched flow — stdout has no destination when launched from a keybind. Direct-CLI form keeps it useful for shell scripts and one-off introspection; hiding from menu prevents the bad-UX path.

  **Outcome of ratifying:** Phase 3.2d lands `Verb { menu_visible: bool }` field addition (every existing `Verb` literal in `REGISTRY` gets `menu_visible: true` — 10 trivial diffs, accounting for the `save-activity` entry that landed in Phase 3.2a), filter wiring in `main.rs::run`, `src/verbs/list_activities.rs` (~15 lines: shell to `jiji-activities list`, print stdout to jiji-do's stdout), registry entry with `menu_visible: false`, a test pinning that `list-activities` does NOT appear in the menu render but DOES appear in `--debug` `kept` output, and a positive shim test pinning `jiji-activities list` is invoked.

**Batching after ratification.** Each box is its own sub-phase (3.2a / 3.2b / 3.2c / 3.2d) — unlike Phase 3.1, these do not share a dispatch shape, and the `cli.rs`/`registry.rs` reshapes for boxes 2 and 4 want isolated review. The architect's plan once all four are ratified:

- **Phase 3.2a (`save-activity`):** smallest; identical pattern to Phase 3.1. Lands first.
- **Phase 3.2d (`list-activities` + `Verb { menu_visible }`):** lands the `Verb`-shape change in isolation so the diff for the field addition is reviewed independently of any new verb body. Lands second.
- **Phase 3.2b (`create-activity` + `menu::prompt_name` + CLI-surface change):** new helper plus the larger CLI-surface decision. Lands third.
- **Phase 3.2c (`remove-activity`):** depends on the CLI surface from 3.2b being settled. Lands fourth.

This ordering minimizes per-sub-phase scope: each lands ~25–60 lines of code plus 2–3 tests, no two boxes share a load-bearing shape change.

---

> Phases 3.2+ implementation is gated on the four ratification boxes above. Once ratified (each box flipped `[x]`, optionally amended), the next `/jiji:land-subphase jiji-do` pass authors Phase 3.2a as the first implementable spec.

### Phase 3.2a — `save-activity` (one tight sub-phase)

Lands the `save-activity` passthrough verb per the Phase 3.2 ratification (commit `0ed347d`) of the first **Proposed** box: snapshot-consuming, identical shape to Phase 3.1's `move-window-to-activity` modulo the snapshot field type (`Option<String>` instead of `Option<u64>`) and the argv shape (positional `save <name>`, not flag `--window=<id>`). Smallest of the four 3.2 sub-phases; lands first per the batching plan.

- [x] `src/verbs/save_activity.rs` — new ~12-line passthrough verb module. Reads `snapshot.focused_activity.as_deref()`; bails with `anyhow::anyhow!("no focused activity at launch")` if `None` (per the Phase 3.1 snapshot-empty contract — exit non-zero, NOT 69). Shells to `jiji-activities save <name>` with the snapshotted activity name as a **positional** argument (`crate::proc::run_capture("jiji-activities", &["save", name])?`). Module rustdoc mirrors `move_window_to_activity.rs` (one paragraph naming the snapshot field read and the bail rationale). Landed in `60175b6`.
- [x] `src/verbs/mod.rs` — add `pub mod save_activity;` in alphabetical position (after `move_workspace_to_activity`). Landed in `60175b6`.
- [x] `src/registry.rs::REGISTRY` — append a 10th `Verb` entry after `assign-workspace`: name `"save-activity"`, label `"Save activity"`, category `Category::Activity`, requires `NIRI_SOCKET | FORK | NIRI_ACTIVITIES` (no FUZZEL — `jiji-activities save` is non-interactive in the default no-`--follow` path), dispatch `verbs::save_activity::run`. Landed in `60175b6`.
- [x] `src/registry.rs::tests::category_grouped_ordering_pins_workspace_before_mode_regardless_of_registration_order` — extend the existing `assert_eq!(names, vec![...])` to include `"save-activity"` as the trailing Activity-category entry. Landed in `60175b6`.
- [x] `src/registry.rs::tests::enabled_with_full_activities_capabilities_includes_all_passthrough_verbs` — add a `names.contains(&"save-activity")` assertion (test name promises "all passthrough verbs"; omission would understate coverage). Landed in `60175b6`.
- [x] `tests/shims.rs::save_activity_passes_focused_activity_name` — new positive shim test mirroring `move_window_to_activity_passes_window_id`. With the default `niri_body` fixture (`focused_activity: Some("acme")`), assert recorded argv contains `"save acme"` (positional, not `--name=acme`). Landed in `60175b6`.
- [x] `tests/shims.rs::save_activity_bails_when_no_focused_activity` — new negative shim test mirroring `move_window_to_activity_bails_when_no_focused_window`. Activities JSON has `is_active: false` everywhere; assert exit `failure()` AND `.code(predicates::ord::ne(69))`, stderr contains `"no focused activity at launch"`, and the `jiji-activities` argv file contains only the `--version` capability-probe line (no dispatch argv). Landed in `60175b6`.
- [x] `tests/shims.rs::debug_reports_filtering_upstream` — extend with `.stdout(predicates::str::contains("save-activity: filtered"))` (the verb requires FORK + NIRI_ACTIVITIES, both absent upstream). Landed in `60175b6`.

**Reviewed:** 2026-05-28 (`60175b6`, was `4bcf1f7` before the fixer amend). Phase 3.2a — `save-activity` snapshot-consuming passthrough verb — landed as one tight sub-phase per the Phase 3.2 batching plan (3.2a → 3.2d → 3.2b → 3.2c; this is the first and smallest, an identical-shape clone of Phase 3.1's pattern modulo `Option<String>` vs. `Option<u64>` snapshot field and positional argv vs. `--flag=<id>`). Reviewed across code quality, silent-failure surface, comment accuracy, test coverage, and type design. Post-review fixes squashed into `60175b6`: `tests/shims.rs::save_activity_bails_when_no_focused_activity` — negative-form argv assertion strengthened to pin that the recorded `jiji-activities` argv contains only the `--version` capability-probe line and no stray `save` dispatch; this is the MEDIUM finding from the pr-test-analyzer (the pre-amend `4bcf1f7` asserted exit-failure and non-69 code but did not verify the subprocess was never dispatched, which is the load-bearing half of the snapshot-empty contract). Same 38 tests green (17 unit + 3 cli + 18 shims; +2 shims vs. Phase 3.1 baseline of 36); `cargo clippy --all --all-targets` zero warnings. Proceed to Phase 3.2d (`list-activities` + `Verb { menu_visible }` field addition) with the reviewed base.

---

### Phase 3.2d — `list-activities` + `Verb.menu_visible` field (one tight sub-phase)

Lands the `Verb { menu_visible: bool }` shape change and the `list-activities` data verb per the Phase 3.2 ratification (commit `0ed347d`) of the fourth **Proposed** box. Second sub-phase in the 3.2a → 3.2d → 3.2b → 3.2c batching order: the `menu_visible` field addition is reviewed in isolation before the two larger CLI-surface sub-phases (3.2b / 3.2c) that add the `menu::prompt_name` helper and name-positional dispatch. `list-activities` is the first and only `menu_visible=false` verb: shells to `jiji-activities list`, forwards stdout verbatim via `print!` (not `println!` — avoids a spurious trailing blank line against `jiji-activities`' per-row `writeln!`).

- [x] `src/verbs/list_activities.rs` — new ~15-line module. Snapshot unused (inventory read fresh inside `jiji-activities`). Calls `proc::run_capture("jiji-activities", &["list"])` and forwards stdout with `print!`. Module rustdoc names the print-vs-println contract. Landed in `5edb863`.
- [x] `src/verbs/mod.rs` — add `pub mod list_activities;` in alphabetical position (after `focus_workspace_previous`, before `move_window_here`). Landed in `5edb863`.
- [x] `src/registry.rs::Verb` — add `menu_visible: bool` field (after `category`, before `requires`). Rustdoc: "False for verbs that are direct-CLI only and must not appear in the fuzzel menu." Landed in `5edb863`.
- [x] `src/registry.rs::REGISTRY` — backfill `menu_visible: true` on all 10 existing `Verb` literals; append 11th entry for `list-activities` with `menu_visible: false`, category `Activity`, requires `NIRI_SOCKET | FORK | NIRI_ACTIVITIES` (no FUZZEL). Landed in `5edb863`.
- [x] `src/registry.rs::enabled_for_menu` — new thin wrapper: calls `enabled(caps)`, filters `.filter(|v| v.menu_visible)`. Rustdoc: "Used by the menu render path only; `enabled()` and `find()` continue to surface menu-hidden verbs so `--debug` and direct CLI dispatch remain unaffected." Landed in `5edb863`.
- [x] `src/main.rs::run` — swap `registry::enabled(caps)` → `registry::enabled_for_menu(caps)` in the menu render path (single-line change). Landed in `5edb863`.
- [x] `src/registry.rs::tests::category_grouped_ordering_pins_workspace_before_mode_regardless_of_registration_order` — extend `assert_eq!(names, vec![...])` to include `"list-activities"` as the trailing Activity-category entry. Landed in `5edb863`.
- [x] `src/registry.rs::tests::enabled_with_full_activities_capabilities_includes_all_passthrough_verbs` — add `names.contains(&"list-activities")` assertion. Landed in `5edb863`.
- [x] `src/registry.rs::tests::enabled_for_menu_hides_menu_hidden_verbs` — new unit test: `Capabilities::all()` → `enabled_for_menu` does not contain `"list-activities"`, `enabled()` does, and a `menu_visible=true` verb (`"save-activity"`) is present in the menu set. Landed in `5edb863`.
- [x] `tests/shims.rs::debug_reports_filtering_upstream` — extend with `.stdout(predicates::str::contains("list-activities: filtered"))`. Landed in `5edb863`.
- [x] `tests/shims.rs::list_activities_forwards_stdout` — positive shim test: `jiji-activities list` shim emits `[{"name":"acme"}]\n`; assert exact stdout bytes `"[{\"name\":\"acme\"}]\n"` (pins that `print!` does not double the newline). Landed in `5edb863`.
- [x] `tests/shims.rs::menu_does_not_render_list_activities` — shim test: fuzzel captures stdin to a file; assert the file does not contain `"List activities"` but does contain `"Switch workspace"`. Pins the `enabled_for_menu` filter wiring end-to-end. Landed in `5edb863`.
- [x] `tests/shims.rs::debug_reports_list_activities_kept_on_fork` — shim test: `--debug` on full-capabilities env asserts `"list-activities: kept"` in stdout (exercising that `enabled()` in `print_debug` is unaffected by the `menu_visible` flag). Landed in `5edb863`.
- [x] `tests/shims.rs::list_activities_capability_miss_exits_69` — fixer-added regression pin: upstream env (no `jiji-activities`, `niri msg --json activities` exits 1) → `jiji-do list-activities` exits 69, stderr contains `"list-activities"`. Landed in `5edb863`.
- [x] `tests/shims.rs::list_activities_subprocess_failure_propagates_nonzero` — fixer-added regression pin: `jiji-activities` shim exits non-zero with stderr message; assert `jiji-do list-activities` exits non-zero (not 69), stderr contains `"compositor unavailable"`. Pins cancel-vs-failure discipline (subprocess failures must not be silently swallowed). Landed in `5edb863`.

**Reviewed:** 2026-05-28 (`5edb863`, was `eadc169` before the fixer amend). Phase 3.2d — `list-activities` data verb and `Verb { menu_visible: bool }` field addition — landed as the second sub-phase in the 3.2 batch, isolating the `Verb`-shape change before the CLI-surface sub-phases. Reviewed across code quality, silent-failure surface, comment accuracy, test coverage, and type design. **Finding worth surfacing:** the load-bearing seam is `enabled_for_menu` as a thin wrapper over `enabled`; a fold of the `menu_visible` filter into `enabled` itself would silently break `--debug` and direct dispatch. Three pins hold this invariant: `enabled_for_menu_hides_menu_hidden_verbs` (unit), `menu_does_not_render_list_activities` (shim), and `debug_reports_list_activities_kept_on_fork` (shim). The rustdoc on `enabled_for_menu` names the invariant explicitly. Post-review fixes squashed into `5edb863`: `tests/shims.rs::list_activities_capability_miss_exits_69` — pins exit-69 on capability miss (not zero, not arbitrary non-zero), protecting the exit-code contract that `--debug` and direct dispatch rely on; `tests/shims.rs::list_activities_subprocess_failure_propagates_nonzero` — pins subprocess failure propagation (non-zero, non-69) per the cancel-vs-failure discipline from `a0eaccc`, preventing a silent-swallow regression. Same 44 tests green (18 unit + 3 cli + 23 shims; +6 vs. Phase 3.2a baseline of 38); `cargo clippy --all --all-targets` zero warnings. Proceed to Phase 3.2b (`create-activity` + `menu::prompt_name` + CLI-surface change) with the reviewed base.

---

### Phase 3.2b — `create-activity` + `menu::prompt_name` + dispatch-signature widening (one tight sub-phase)

Lands the `create-activity` passthrough verb per the third **Proposed** box of the Phase 3.2 ratification (commit `0ed347d`). Third sub-phase in the 3.2a → 3.2d → 3.2b → 3.2c batching order: introduces the freeform-name fuzzel prompt helper plus the verb-dispatch signature widening (`fn(&Snapshot) → fn(&Snapshot, Option<&str>)`) needed for any verb that consumes a CLI-positional name. Lands now because Phase 3.2c (`remove-activity`) depends on both pieces being settled.

**Cross-cutting decision baked in (architect's call within the ratified shape).** Option (b) of the ratification box — a second positional in `Cli` — combined with widening the `Verb::dispatch` function pointer from `fn(&Snapshot)` to `fn(&Snapshot, Option<&str>)`. Rejected alternatives: (i) `&Cli` parameter (couples every verb to the CLI struct shape and breaks the "registry as single source of truth" framing — the registry currently knows nothing about clap), (ii) thread-local / static side channel (anti-pattern; surfaces hidden state at the dispatch boundary), (iii) per-verb dispatch trait (over-engineered at one positional-name verb). The `Option<&str>` widening is the smallest reach that does the job: all 11 existing verbs gain a `_: Option<&str>` ignore in their signature (trivial mechanical diffs), the call site in `main.rs::run` passes `args.verb_arg.as_deref()`, and a future second-arg verb upgrades to a `VerbArgs` struct without rewriting any existing verb. The `Option<&str>` is positional-only by clap convention; flag-style args land via a separate widening if/when a verb needs them.

- [x] `src/cli.rs::Cli` — add `verb_arg: Option<String>` as a second positional after `verb`. Update rustdoc on the existing `verb` field if it mentions "the only positional"; add a new doc line on `verb_arg`: `"Optional positional argument for verbs that accept a name (e.g. \`create-activity <name>\`). Verbs that ignore it work as before."` No `clap(name = ...)` needed — positional order suffices. Landed in `92888e4`.
- [x] `src/registry.rs::Verb::dispatch` — widen the function pointer from `fn(&Snapshot) -> anyhow::Result<()>` to `fn(&Snapshot, Option<&str>) -> anyhow::Result<()>`. Update the field rustdoc to name the new parameter: `"the optional positional CLI arg (e.g. the name for \`create-activity <name>\`); \`None\` for menu invocation or when the verb takes no positional"`. Landed in `92888e4`.
- [x] `src/registry.rs::tests::sort_by_key_preserves_intra_category_registration_order` — update the local `noop_dispatch` test fixture from `fn(_: &Snapshot) -> Result<()>` to `fn(_: &Snapshot, _: Option<&str>) -> Result<()>` so the test still compiles. Landed in `92888e4`.
- [x] `src/verbs/*.rs` — update all 11 existing verb `run` signatures from `fn run(snapshot: &Snapshot)` to `fn run(snapshot: &Snapshot, _arg: Option<&str>)`. The 11 modules are: `assign_workspace`, `focus_workspace_previous`, `list_activities`, `move_window_here`, `move_window_to_activity`, `move_workspace_to_activity`, `save_activity`, `switch_activity`, `switch_activity_previous`, `switch_workspace`, `toggle_debug_tint`. All ignore the new parameter — mechanical addition. Module rustdocs need no update (the parameter is ignored). Landed in `92888e4`.
- [x] `src/main.rs::run` — at the two dispatch call sites (direct dispatch in the `Some(name)` arm; menu dispatch in the `None` arm), change `(verb.dispatch)(&snapshot)` to `(verb.dispatch)(&snapshot, args.verb_arg.as_deref())`. The menu arm passes `args.verb_arg.as_deref()` even though menu invocation conventionally has no positional — clap permits `jiji-do verb_arg_only` only via the `verb` slot, so menu mode always sees `verb_arg = None` (no behavioral change for the menu path). Landed in `92888e4`.
- [x] `src/menu.rs::prompt_name` — new helper, ~30 lines, near-clone of jiji-activities' `picker::single_select::prompt_name` (`repos/jiji-activities/src/picker/single_select.rs:212`). Signature: `pub fn prompt_name(prompt: &str) -> anyhow::Result<Option<String>>`. Spawns `fuzzel --dmenu --prompt <prompt>` with stdin piped and immediately dropped (sends EOF — fuzzel `--dmenu` reads the candidate list to EOF before drawing, so empty stdin = free-text prompt). Cancel-vs-failure discipline exactly matches the existing `menu::pick_one` (lines 36–46): `out.status.success()` + non-empty trimmed first line → `Ok(Some(name))`; success + empty → `Ok(None)`; exit code 1 → `Ok(None)` (user-cancel convention); any other non-zero or signal → `bail!("fuzzel failed (exit {}): {}", ...)`. Both the success-empty and exit-1-cancel cases collapse to `Ok(None)` per the DD: the caller treats both as "no name supplied" → exit 0 no-op. Rustdoc names both the cancel-vs-failure discipline and the empty-stdin/free-text contract. Landed in `92888e4`.
- [x] `src/verbs/create_activity.rs` — new module, ~25 lines. Signature: `pub fn run(_snapshot: &Snapshot, arg: Option<&str>) -> anyhow::Result<()>`. Logic: (a) if `arg` is `Some(name)` and non-empty after trim → shell directly to `jiji-activities create <name>` via `crate::proc::run_capture("jiji-activities", &["create", name])`. (b) Otherwise (no positional OR explicitly empty positional) → call `crate::menu::prompt_name("Activity name: ")?`; on `Some(typed)` shell to `jiji-activities create <typed>`; on `None` (cancel or empty Enter) return `Ok(())` (clean no-op, exit 0). Snapshot unused — the verb does not act on the focused context. Module rustdoc names: "freeform-name verb; direct-CLI path skips the prompt, menu path prompts via fuzzel `--dmenu` with empty stdin". Empty positional in the direct-CLI form (a trim that empties) routes to (b) so `jiji-do create-activity ""` from a script prompts rather than silently sending an empty `create` to `jiji-activities`. Landed in `92888e4`.
- [x] `src/menu.rs::tests` — new unit test `prompt_name_decode_outcomes` (or similar pure-function tests if `prompt_name` factors out a `decode(success, stdout) -> Option<String>` helper as `single_select.rs::decode_name_outcome` does — implementer's judgment whether to factor; the four-case table is small enough to inline). Pin: (success, "name\n") → `Some("name")`; (success, "  \n") → `None`; (success, "") → `None`; (false, anything) → returns the bail branch (handled at integration-test layer if not factored). If kept inline in `prompt_name`, this test is omitted and coverage relies on the shim layer. Landed in `92888e4` (coverage via shim layer; `prompt_name` kept inline without a factored decode helper — threshold not reached).
- [x] `src/registry.rs::REGISTRY` — append 12th `Verb` entry for `create-activity` after `list-activities`: name `"create-activity"`, label `"Create activity"`, category `Category::Activity`, `menu_visible: true`, requires `NIRI_SOCKET | FUZZEL | FORK | NIRI_ACTIVITIES` (per the ratification box — FUZZEL required even for direct-CLI form to stay simple; the conditional-FUZZEL nuance is parked in Appendix C per the architect note below), dispatch `verbs::create_activity::run`. Landed in `92888e4`.
- [x] `src/verbs/mod.rs` — add `pub mod create_activity;` in alphabetical position (after `assign_workspace`, before `focus_workspace_previous`). Landed in `92888e4`.
- [x] `src/registry.rs::tests::category_grouped_ordering_pins_workspace_before_mode_regardless_of_registration_order` — extend the trailing `assert_eq!(names, vec![...])` to include `"create-activity"` as the new last Activity-category entry (after `"list-activities"`). Landed in `92888e4`.
- [x] `src/registry.rs::tests::enabled_with_full_activities_capabilities_includes_all_passthrough_verbs` — add `assert!(names.contains(&"create-activity"))`. Landed in `92888e4`.
- [x] `tests/shims.rs::create_activity_direct_cli_skips_prompt_and_passes_name` — new positive shim test: pass `--arg` positional `jiji-do create-activity foo`; the `jiji-activities` shim records argv; the `fuzzel` shim is set to fail on invocation (`exit 99; echo "fuzzel should not be called" >&2`) so a regression where direct-CLI accidentally calls the prompt is caught. Assert recorded argv contains `"create foo"`; assert no fuzzel-was-called marker. Landed in `92888e4`.
- [x] `tests/shims.rs::create_activity_menu_path_prompts_and_passes_typed_name` — new positive shim test: invoke `jiji-do create-activity` with no positional; the `fuzzel` shim echoes a name (`echo 'newact'`); assert recorded `jiji-activities` argv contains `"create newact"`. Landed in `92888e4`.
- [x] `tests/shims.rs::create_activity_cancel_exits_zero_no_dispatch` — new negative shim test: invoke `jiji-do create-activity` (no positional); `fuzzel` shim exits 1 (cancel); assert exit 0 AND no `create` argv recorded for `jiji-activities` (only the `--version` capability-probe line per the established argv-probe-accounting pattern from `move_window_to_activity_bails_when_no_focused_window`). Landed in `92888e4`.
- [x] `tests/shims.rs::create_activity_empty_prompt_exits_zero_no_dispatch` — new negative shim test: invoke `jiji-do create-activity` (no positional); `fuzzel` shim exits 0 with empty stdout (user pressed Enter without typing); assert exit 0 AND no `create` argv recorded. Landed in `92888e4`.
- [x] `tests/shims.rs::create_activity_fuzzel_failure_propagates_nonzero` — new shim test pinning cancel-vs-failure discipline (parallel to `switch_workspace_fuzzel_failure_exits_nonzero`): invoke `jiji-do create-activity` (no positional); `fuzzel` shim prints to stderr and exits 2; assert exit non-zero (NOT 69, NOT 0), stderr contains `"fuzzel failed"`. Landed in `92888e4`.
- [x] `tests/shims.rs::debug_reports_filtering_upstream` — extend with `.stdout(predicates::str::contains("create-activity: filtered"))`. Landed in `92888e4`.
- [x] `tests/shims.rs::create_activity_empty_positional_routes_to_prompt` — negative shim test pinning the empty-positional normalization contract: `jiji-do create-activity ""` must route to fuzzel rather than dispatching an empty name, ensuring the `.filter(|s| !s.is_empty())` guard stays in place. **Fixer-added** after initial review (`1f3f84b`) identified the missing negative pin for Hazard 5 (empty-positional silent-dispatch trap); squashed into `92888e4`. Landed in `92888e4`.

**Architect note (parked, not gated).** The DD's ratified capability set requires `FUZZEL` even for the direct-CLI form (`jiji-do create-activity foo` errors at exit 69 if fuzzel is missing on PATH, despite no prompt firing in that path). This matches the ratification verbatim and keeps the registry's static capability set simple — verbs with conditional capability requirements would need a verb-level "effective requires" computation that doesn't exist today. The conditional-FUZZEL nuance is recorded in Appendix C as a deferred suggestion so a future architect doesn't re-litigate the same trade-off.

**Reviewed:** 2026-05-28 (`92888e4`, was `1f3f84b` before the fixer-squash). Phase 3.2b — `create-activity` passthrough verb plus `menu::prompt_name` freeform-name helper plus `Verb::dispatch` signature widening from `fn(&Snapshot)` to `fn(&Snapshot, Option<&str>)` — landed as one tight sub-phase per the 3.2a → 3.2d → 3.2b → 3.2c batching order. Reviewed across code quality, silent-failure surface, comment accuracy, test coverage, and type design. **Finding worth surfacing (empty-positional normalization test gap, `tests/shims.rs`):** the initial commit (`1f3f84b`) covered the positive direct-CLI, menu-path, cancel, empty-prompt, and fuzzel-failure paths (five shim tests) but lacked a negative pin for Hazard 5 — the empty-positional silent-dispatch trap (`jiji-do create-activity ""`). The normalization guard `.filter(|s| !s.is_empty())` in `create_activity::run` is the load-bearing invariant ensuring an explicitly empty positional routes to `prompt_name` rather than forwarding an empty string to `jiji-activities create`. Without the negative pin, removing that guard produces no test failure. The fixer squashed `create_activity_empty_positional_routes_to_prompt` into the original commit, yielding `92888e4`. The test checks that the `jiji-activities` argv record contains no `create` line and that fuzzel was invoked, discriminating a dispatch-before-prompt regression from a clean prompt-routing path. **Finding worth surfacing (dispatch-signature widening as structural inflection point, `src/registry.rs`):** the `fn(&Snapshot, Option<&str>)` widening is the load-bearing shape for all future positional-name verbs. `remove-activity` (Phase 3.2c) is the next verb that uses `verb_arg`; a second positional-name verb after that triggers the upgrade path to `VerbArgs` struct. The crossing point is now spec'd in the Phase 3.2b preamble and Appendix C. **DD also amended in this commit:** restored the missing `## Appendix C: Deferred Suggestions` section header, which was accidentally displaced when `55eef38` inserted the Phase 3.2b ledger block in place of the old header position. Post-review fixes squashed into `92888e4`: `tests/shims.rs::create_activity_empty_positional_routes_to_prompt` added at `tests/shims.rs` (Hazard 5 negative pin for the `.filter(|s| !s.is_empty())` normalization guard). Same 50 tests green (18 unit + 3 cli + 29 shims; +6 shims vs. Phase 3.2d baseline of 44: 5 spec'd shim tests + 1 fixer-added); `cargo clippy --all --all-targets` zero warnings; `cargo +nightly fmt --all` clean. Proceed to Phase 3.2c (`remove-activity`) with the reviewed base.

---

### Phase 3.2c — `remove-activity` (one tight sub-phase)

Lands the `remove-activity` passthrough verb per the third **Proposed** box of the Phase 3.2 ratification (commit `0ed347d`). Fourth and final sub-phase in the 3.2a → 3.2d → 3.2b → 3.2c batching order: depends on Phase 3.2b's `verb_arg: Option<String>` positional and the established `Verb::dispatch` signature `fn(&Snapshot, Option<&str>)`. Shape: direct-CLI path accepts `verb_arg` and shells straight through; menu path reads the activity inventory live at dispatch time (not from `Snapshot` — per the Phase 3.2 cross-cutting decision) and runs `menu::pick_one`. Architecturally-resolved call: bail with `anyhow::bail!("no activities to remove")` (exit 1, NOT 69) *before* `menu::pick_one` if the inventory is empty — a runtime-data miss, not a capability miss.

- [x] `src/verbs/remove_activity.rs` — new 46-line module. Private `NameBrief { name: String }` deserializer (intentionally discards `is_active` — picker shows all activities); module rustdoc names the bail-before-fuzzel position and the snapshot-purity rationale for live inventory read. Direct-CLI path: `arg.map(str::trim).filter(|s| !s.is_empty())` normalization (parallel to Phase 3.2b's create-activity guard), then `crate::proc::run_capture("jiji-activities", &["remove", name])?`. Menu path: read `niri msg --json activities`, parse into `Vec<NameBrief>`, bail with `"no activities to remove"` if empty, collect names, call `crate::menu::pick_one("activity", &names)?`, dispatch on `Some(picked)`. Landed in `a1c9bee`.
- [x] `src/verbs/mod.rs` — add `pub mod remove_activity;` in alphabetical position (after `move_workspace_to_activity`, before `save_activity`). Landed in `a1c9bee`.
- [x] `src/registry.rs::REGISTRY` — append 13th `Verb` entry after `create-activity`: name `"remove-activity"`, label `"Remove activity"`, category `Category::Activity`, `menu_visible: true`, requires all four-flag cluster (`NIRI_SOCKET | FUZZEL | FORK | NIRI_ACTIVITIES`), dispatch `verbs::remove_activity::run`. Landed in `a1c9bee`.
- [x] `src/registry.rs::tests::category_grouped_ordering_pins_workspace_before_mode_regardless_of_registration_order` — extend trailing `assert_eq!(names, vec![...])` to include `"remove-activity"` as the new last Activity-category entry (after `"create-activity"`). Landed in `a1c9bee`.
- [x] `src/registry.rs::tests::enabled_with_full_activities_capabilities_includes_all_passthrough_verbs` — add `names.contains(&"remove-activity")` assertion. Landed in `a1c9bee`.
- [x] `tests/shims.rs::remove_activity_direct_cli_skips_picker_and_passes_name` — positive direct-CLI shim test: `jiji-do remove-activity work`; sabotaged fuzzel shim (exit 99) makes any accidental picker invocation loud; assert recorded `jiji-activities` argv contains `"remove work"`. Landed in `a1c9bee`.
- [x] `tests/shims.rs::remove_activity_menu_path_picks_and_passes_picked_name` — positive menu-path shim test: two-activity inventory (`work`/`play`, mixing `is_active: true/false`) confirming picker shows all without `is_active` filtering; fuzzel shim echoes `"play"`; assert argv contains `"remove play"`. Landed in `a1c9bee`.
- [x] `tests/shims.rs::remove_activity_cancel_exits_zero_no_dispatch` — fuzzel exit-1 cancel → exit 0; assert only `--version` capability-probe line in argv (per the probe-accounting pattern from Phase 3.1). Landed in `a1c9bee`.
- [x] `tests/shims.rs::remove_activity_empty_positional_routes_to_picker` — pins the `.filter(|s| !s.is_empty())` normalization: `jiji-do remove-activity ""` → fuzzel picker (not silent `remove ""` dispatch); fuzzel cancel (exit 1) → exit 0; only `--version` probe in argv. Parallel to Phase 3.2b's `create_activity_empty_positional_routes_to_prompt`. Landed in `a1c9bee`.
- [x] `tests/shims.rs::remove_activity_empty_inventory_bails_before_fuzzel` — architecturally-load-bearing test: `niri msg --json activities` returns `[]` → exit 1 (NOT 69; `.code(predicates::ord::ne(69))`), stderr `"no activities to remove"`, sabotaged fuzzel (exit 99) makes accidental spawn loud, only `--version` probe in argv. This test is the primary guard on the bail-before-fuzzel position. Landed in `a1c9bee`.
- [x] `tests/shims.rs::debug_reports_filtering_upstream` — extend with `.stdout(predicates::str::contains("remove-activity: filtered"))`. Landed in `a1c9bee`.

**Reviewed:** 2026-05-28 (`a1c9bee`). Phase 3.2c — `remove-activity` inventory-picker passthrough verb — landed as the fourth and final sub-phase in the 3.2a → 3.2d → 3.2b → 3.2c batching plan, closing Stage 3. Clean first-pass review; all 7 named hazards passed with direct test coverage. Reviewed across code quality, silent-failure surface, comment accuracy, test coverage, and type design. **Finding worth surfacing (empty-inventory bail-before-fuzzel position, `src/verbs/remove_activity.rs:30–32`):** the architecturally-resolved call to bail *before* `menu::pick_one` when the activities inventory is empty is load-bearing — falling through to fuzzel with an empty candidate list would let the user type an unconstrained string and forward it as a `jiji-activities remove` argument, bypassing the picker's purpose and silently dispatching unvalidated input. The bail position is guarded by `remove_activity_empty_inventory_bails_before_fuzzel` with a sabotaged fuzzel shim (exit 99) that makes any accidental picker spawn loud as a test failure. Exit 1 (NOT 69) correctly classifies this as a runtime-data miss vs. a capability miss — an invariant in the same family as Phase 3.1's snapshot-empty contract and `a0eaccc`'s fuzzel cancel-vs-failure discipline. **Finding worth surfacing (inventory-read-at-dispatch-time vs. snapshot, `src/verbs/remove_activity.rs`):** this verb is the first consumer of the Phase 3.2 cross-cutting decision to read the activities inventory live at dispatch time rather than in `Snapshot::capture`. The decision is structurally correct: `Snapshot` is "focused state at process start"; the activities inventory is live state that may change during a long-running menu session. The commit message documents the rationale; the `NameBrief` private-to-module deserializer (discarding `is_active`) keeps the concern self-contained without polluting `snapshot.rs`. Post-review fixes squashed into `a1c9bee`: none needed in the commit itself. Same 55 tests green (18 unit + 3 cli + 34 shims; +5 shims vs. Phase 3.2b baseline of 50); `cargo clippy --all --all-targets` zero warnings. Stage 3 structurally complete (all 9 activities passthrough verbs landed across Phase 2.1 + Phase 3.1 + Phases 3.2a–3.2d); proceed to Stage 4 (polish: README + fish completions + packaging per Launcher initiative §4) with the reviewed base.

---

### Phase 4.0 — Stage 4 polish design ratification (human gate)

Stage 4 closes the `jiji-do` v0.1.0 story per the Launcher initiative §4 Stage 4 bullets: README, fish completions, chezmoi hash bump, `--version` / `--help` polish, top-level keybinding example block. Three of the five are mechanical (chezmoi hash bump, `--version`, `--help` "list categories" — fold into the eventual implementation sub-phase). The other two carry UX/architectural calls that fall outside the architect's lane and warrant a human gate, parallel to Phase 2.0 and Phase 3.2 — README shape is a voice/audience call the architect can only draft; the fish-completions strategy is a genuine architectural reshape question because the current `cli::Cli` surface (`verb: Option<String>` + `verb_arg: Option<String>` — two opaque positionals) gives `clap_complete` no verb-list to complete against, and any non-degenerate completion needs the registry's verb names lifted into the clap surface.

Each box below is a `**Proposed:**` gate — ratify in-place by flipping `[ ]` → `[x]`, optionally with an amendment note. Drafts below are the architect's recommended defaults; nothing is decided until a box is flipped.

- [x] **Proposed: README shape and audience.** _Ratified 2026-06-02 as proposed._ Audience is a fork-running user setting up the launcher for the first time — assumes they know fuzzel/niri/wayland but not `jiji-do`. Sections in this order: (1) **What is `jiji-do`** — one paragraph framing it as Helix-style command launcher with snapshot-at-launch context capture, links the launcher-initiative doc and DD. (2) **Capability matrix** — table of the four capabilities (`NIRI_SOCKET`, `FUZZEL`, `FORK`, `NIRI_ACTIVITIES`) × what's enabled when present / what's hidden when absent (mirrors the initiative §5 capability-model table, slightly trimmed). (3) **Install** — `cargo install --path .` from a clone (no remote yet — Phase D pending, called out explicitly so users don't search for it on crates.io); runtime deps (`fuzzel` mandatory; `jiji-activities` optional, unlocks activities verbs; jiji fork optional, unlocks fork-only actions). (4) **Usage** — `jiji-do` (menu), `jiji-do <verb>` (direct), `jiji-do --debug` (diagnostic), the named verbs grouped by category with one-line descriptions lifted from `Verb::label`. (5) **Example keybindings** — see the third box below; copies its decision verbatim. (6) **Dependency contract** — one paragraph reiterating the "no `niri-ipc` / `jiji-activities` Cargo dep, all subprocess" stance and why (cross-fork compatibility). (7) **Contributing / status** — pre-1.0, design in `docs/design.md`, owned by the unified jiji loop. No screenshots (fuzzel renders are minimal and would need refreshing on every config change); no animated GIFs.

  **Rationale:** the launcher's value proposition is invisible without the capability-matrix framing (a user installing on upstream niri without `jiji-activities` needs to understand *why* their menu is short and what the path to full functionality looks like). Audience-first ordering (what-is → capability → install → usage) over reference-first (here are all 13 verbs) keeps the README scannable for the first-time user. The Stage-4 boxes' "fish completions" and "keybind examples" decisions are referenced from the README rather than re-spelled, so the README stays maintenance-light when those decisions are amended.

  **Outcome of ratifying:** Phase 4.1 lands `README.md` (~150–200 lines per the section list above), folds in the mechanical Stage 4 bullets (chezmoi hash bump, `--version` already works, `--help` clap-derive output already lists subcommands by name — "list categories" reads as "group `--help` output by `Category`" which is a separate clap shape change deferred to Phase 4.2 if the second box below lands the subcommand-enum reshape, otherwise dropped from v0.1.0 as low-value).

- [x] **Proposed: Fish completion strategy — `clap_complete` static base via a `PossibleValuesParser` lift from the registry; no dynamic verb_arg augmentation in v0.1.0.** _Ratified 2026-06-02 as proposed: option (a). **Re-ratified 2026-06-02: option (b).**_ The current `cli::Cli` has `verb: Option<String>` + `verb_arg: Option<String>` — both opaque to clap. The registry is the source of truth for verb names but clap doesn't know about it. Three options canvassed:

  - **(a) Lift verb names into a `value_parser = PossibleValuesParser::new(<lifted from REGISTRY>)`** on the existing `verb: Option<String>` field. Smallest reach: one `lazy_static!` (or `const fn`-style) array of names assembled from `REGISTRY`, fed to `PossibleValuesParser`. clap_complete generates a static name list per shell from this. Leaves `verb_arg: Option<String>` opaque (no completion for the second positional) — which is the correct v0.1.0 choice because the two verbs that consume `verb_arg` have heterogeneous shapes: `create-activity <name>` is freeform (no completion candidates exist); `remove-activity <name>` is inventory-driven (would need `jiji-do` to shell to `niri msg --json activities` at tab time, mirroring the jiji-activities `FISH_NAMES_CMD` pattern). The dynamic-augmentation pattern is a real ask but deferred — it doubles the completion code's complexity for two verbs in a binary where the menu path is the discoverability primary.

  - **(b) Reshape `Cli` into a proper `#[derive(Subcommand)]` enum** with one variant per verb (mirrors `jiji-activities`'s `Cmd` enum). Large diff: every verb-dispatch test in `tests/cli.rs` / `tests/shims.rs` that invokes `jiji-do <verb>` continues to work (the string surface is unchanged) but `main.rs::run` rewrites from `match args.verb { Some(name) => registry::find(&name) ... }` to `match args.cmd { Cmd::SwitchWorkspace => ..., Cmd::CreateActivity { name } => ..., ... }`. Breaks the "registry is the single source of truth" invariant — the verb list lives in two places (registry array + Cmd enum) and must be kept in sync. The dispatch function pointer in `Verb` becomes redundant; the registry collapses to metadata only. This is a meaningful re-architecture and deserves its own sub-phase if pursued.

  - **(c) Hand-roll fish completion entirely**, not via `clap_complete`. Most code; only useful if dynamic verb_arg completion is the primary requirement. Rejected — dynamic completion is the deferred-feature path, not the v0.1.0 path.

  Architect recommends **(a)**. Smallest reach, gives the user tab-completable verb names (which is the 80%-of-the-value), preserves the registry-as-single-source-of-truth invariant, leaves the path to (b) or dynamic augmentation open as a Stage-5+ amendment. The lift mechanism: `REGISTRY.iter().map(|v| v.name).collect::<Vec<_>>()` as a `LazyLock<Vec<&'static str>>` (Rust 1.80+ — already in `edition = "2024"`), fed to a `clap::builder::PossibleValuesParser::new(NAMES.iter().copied())`. clap's hidden-from-help-but-completable behavior is the default for `PossibleValuesParser` so the static name set lands in fish completion without polluting `--help`.

  **Amendment — re-ratified 2026-06-02 to option (b).** Option (a) landed at `0f9a04a` and its ratified rationale ("gives the user tab-completable verb names") was **empirically falsified for fish — the project's shell**. clap_complete's fish generator does **not** emit a positional argument's `PossibleValuesParser` values as completions; it only emits real subcommands. Proof: at `0f9a04a`, `jiji-do completions fish | grep -c switch-activity` → `0`, while `jiji-do completions bash | grep -c switch-activity` → `1`. By contrast `jiji-activities` (which models verbs as a real `#[derive(Subcommand)] Cmd` enum) **does** emit fish verb-name completions — `complete -c jiji-activities -n "__fish_jiji_activities_needs_command" -f -a "assign-workspace" …`. Option (a) therefore delivers zero of its 80%-of-the-value for the actual target shell. The decision switches to **option (b): reshape `jiji-do`'s verbs into a per-verb `#[derive(Subcommand)]` enum** so the fish generator emits verb-name completions. The "registry collapses to metadata only" concern raised in option (b)'s original analysis is **not** adopted — see Phase 4.2's resolved central design call below: the `Cmd` enum is a **thin completion-and-parse layer** that maps each variant to its canonical kebab-case name and **delegates dispatch back to `registry::find(name)`**, so the registry stays the single source of truth for labels, categories, capability sets, `menu_visible`, and the dispatch fn pointer. A loud parity test (`cmd_variants_match_registry`) enforces enum↔registry sync, the option-(b) analysis's chief risk. The option (a)/(b)/(c) analysis above is preserved as the decision record.

  **Outcome of ratifying:** Phase 4.2 lands `src/cli.rs` `value_parser` augmentation (~10 lines), a `src/completions.rs` module modeled on `repos/jiji-activities/src/completions.rs` lines 62–72 (the static-only path; no dynamic `emit_fish_dynamic` since we're explicitly not doing dynamic v0.1.0), `Cmd::Completions { shell: Shell }` variant addition to the CLI (which itself requires the `Subcommand` enum reshape — but only one variant, namely `Completions`, while keeping `verb` / `verb_arg` as the default-when-no-subcommand fallback via clap's `args_conflicts_with_subcommands` or equivalent escape hatch). The `Completions` subcommand introduces the *first* clap-subcommand in `jiji-do` and forces a small `Cli` restructure: `Cli { #[command(subcommand)] cmd: Option<Cmd>, verb: Option<String>, verb_arg: Option<String> }` with `Cmd::Completions { shell }` as the only variant. If the implementer judges this restructure unergonomic, the alternative is a global `--completions <shell>` flag on `Cli` (zero subcommand surface, smaller diff but less clap-idiomatic). Implementer's call inside the ratified shape.

- [x] **Proposed: Default keybind examples for the README — copy initiative §7 verbatim with two amendments.** _Ratified 2026-06-02 as proposed (amended block)._ The launcher initiative §7 spec'd four example keybinds: `Mod+colon` (menu), `Mod+W` (switch-workspace), `Mod+A` (switch-activity), `Mod+Shift+W` (move-window-to-workspace). Architect recommends:

  - Keep `Mod+colon` for the menu. `Mod+colon` is unbound in the current standard config (verified against `~/.local/share/chezmoi/dot_config/niri/config.kdl.tmpl`) and the colon-key affordance for "command palette" is a borrowed Vim/Helix convention that reads naturally.
  - **Drop `Mod+W` for switch-workspace.** `Mod+W` is bound in the standard config to `consume-window-into-column` (workspace nav uses `Mod+PgUp/Dn`, `Mod+K/J`, `Mod+WheelScroll`). Replace with `Mod+Tab` or leave unmapped in the example block — the user picks. Note in the README that the example block is example only, not a default install action.
  - **Drop `Mod+A` for switch-activity** unless the user wants to remap (currently `Mod+A` is bound to `spawn anyrun`). Same "example only, user picks" framing.
  - Drop `Mod+Shift+W` for move-window-to-workspace — Stage 2's restoration of fuzzel-picker `move-window-to-workspace` was cut at Phase 2.0 ratification (Appendix D), so the example targets a verb that doesn't exist in v0.1.0. The launcher's analog `move-window-to-activity` is fork-only and gated on activities capability.
  - Add `Mod+Space` or similar example for the fuzzel menu path as an alternative to `Mod+colon` for users on US-only layouts where `colon` requires shift.

  **Rationale:** the initiative §7 keybinds were drafted pre-Phase 2.0 ratification, when the verb set was assumed broader. Now that Stage 2's surviving verb roster is two (`focus-workspace-previous`, `toggle-debug-tint`) and Stage 3 is activities-only, two of the four example keybinds aim at verbs that either don't exist or collide with the standard config. The README block should illustrate, not prescribe — a single confident example (`Mod+colon` → menu; menu does everything) plus an "advanced: bind individual verbs" subsection with one or two examples is more honest than four examples half of which need amendment.

  **Outcome of ratifying:** the README's "Example keybindings" section (per the first box's outline) carries the amended block: `Mod+colon` → menu (one example), plus a "bind verbs directly" subsection with one example each for native (`focus-workspace-previous`) and passthrough (`switch-activity`) cases. The initiative §7 example block gets a one-line amendment in a follow-up scribe pass noting "see jiji-do README for the v0.1.0 example keybinds; §7 below is pre-Phase-2.0 historical."

- [x] **Proposed: Stage 4 batching.** _Ratified 2026-06-02 as proposed: two sub-phases, chezmoi hash bump deferred to 4.2._ Two sub-phases:

  - **Phase 4.1 (README + mechanical polish).** Lands `README.md` per box 1's outline, the keybind examples per box 3, and the mechanical Stage 4 bullets (chezmoi hash bump for fish completions after Phase 4.2 lands — sequencing matters here, see below; `--version` already works via clap-derive; `--help` "list categories" deferred or dropped per box 1 outcome). No code touched in `src/`. Fits inside one commit (~200 lines of markdown).
  - **Phase 4.2 (completions infrastructure).** Lands the `cli::Cli` `value_parser` augmentation, the `src/completions.rs` module, the `Cmd::Completions { shell }` subcommand wiring (or `--completions <shell>` global flag — implementer's call), and one shim test pinning that `jiji-do completions fish` (or `jiji-do --completions fish`) emits a non-empty fish script containing the verb names. Smaller diff (~50–100 lines of code + ~30 lines of test) but lands an architecturally-load-bearing change (the first clap subcommand or the first global value-flag on `Cli`). Lands **after** 4.1 so the README documenting the completion install command is not pointing at non-existent code at any commit boundary.

  **Sequencing nuance:** the chezmoi `# hash:` bump (a Stage 4 mechanical bullet) belongs in Phase 4.1's README commit because the chezmoi script's `jiji-do completions fish` invocation only becomes a real call after 4.2 lands — but 4.1 lands first, so the bump in 4.1 is forward-looking (the chezmoi script's invocation of `jiji-do completions fish` will exit non-zero until 4.2 lands, which is acceptable as a transient state: the user reinstalls jiji-do after 4.2 and runs `chezmoi apply` once, the hash is already bumped, and the fish completion generates on the next apply). Alternative: defer the hash bump to 4.2's commit. Architect recommends **defer to 4.2** — the hash bump is meaningless without the binary supporting `completions fish`, and bundling the README with a forward-looking infrastructure change feels less clean than letting 4.2 own its full deployment story. README's install instructions in 4.1 cover the manual `jiji-do completions fish > ~/.config/fish/completions/jiji-do.fish` path as the v0.1.0 install discipline; the chezmoi automation is a Phase 4.2 deliverable.

  **Outcome of ratifying:** the architect drafts Phase 4.1 as the first implementable spec immediately after this gate clears; Phase 4.2 follows once 4.1 is reviewed. Both sub-phases are `Default` complexity (no Mechanical claims, no Deep escalation needed — straightforward feature work against a well-understood codebase).

---

### Phase 4.1 — README + keybind examples (one sub-phase, docs-only)

Lands `README.md` per the ratified box 1 seven-section outline, with the "Example keybindings" section carrying the ratified box 3 amended block. Docs-only: no `src/` code, no test, no chezmoi touch (the chezmoi `# hash:` bump and `completions` install automation are deferred to Phase 4.2 per the ratified box 4 sequencing decision). The README documents the **manual** completion-install path (`jiji-do completions fish > …`) as a forward reference, framed as "available after Phase 4.2 / once the `completions` subcommand lands" so no commit boundary points at non-existent code as a live instruction — the install section's completion line is phrased as the post-4.2 step, not a present-tense one.

Authoritative sources for content (do not re-derive from the source — lift verbatim where the box specifies):
- Verb names / labels / categories / capability sets: `src/registry.rs::REGISTRY` (13 entries; `list-activities` is `menu_visible: false` — it is a direct-CLI data verb, document it under Usage but note it does not appear in the menu).
- The four capabilities and their probe semantics: `src/capabilities.rs` (`NIRI_SOCKET`, `FUZZEL`, `FORK`, `NIRI_ACTIVITIES`; each probed independently, a miss just leaves the flag unset).
- CLI surface (`jiji-do`, `jiji-do <verb>`, `jiji-do <verb> <arg>`, `jiji-do --debug`): `src/cli.rs` + `src/main.rs::run`.
- Capability-model table to mirror (trimmed): `docs/launcher/initiative.md` §5.
- Keybind examples amended block: Phase 4.0 box 3 above (ratified verbatim).

- [x] `README.md` — new file (~150–200 lines markdown). Seven sections in the box-1 order: (1) **What is `jiji-do`** — one paragraph; Helix-style command launcher with snapshot-at-launch context capture; link `docs/launcher/initiative.md` (workspace) and `docs/design.md` (this DD). (2) **Capability matrix** — table mirroring initiative §5 (trimmed): one row per capability (`NIRI_SOCKET`, `FUZZEL`, `FORK`, `NIRI_ACTIVITIES`), columns = what it gates / what is hidden when absent. (3) **Install** — `cargo install --path .` from a clone; call out explicitly that there is no remote / crates.io listing yet (Phase D pending) so users do not search for it; runtime deps (`fuzzel` mandatory for the menu; `jiji-activities` optional → unlocks activities verbs; jiji fork optional → unlocks fork-only actions). Completion-install line phrased as a post-4.2 forward reference. (4) **Usage** — `jiji-do` (menu), `jiji-do <verb>` (direct), `jiji-do <verb> <arg>` (verbs that accept a name, e.g. `create-activity`), `jiji-do --debug` (diagnostic, prints resolved capabilities + per-verb kept/filtered). Then the verbs grouped by `Category` (Workspace, Window, Mode, Activity) with the one-line `label` for each; note `list-activities` is direct-CLI only (not in the menu). (5) **Example keybindings** — the box-3 amended block verbatim: `Mod+colon` → menu as the single confident example with "example only, not a default install action" framing; a `Mod+Space` alternative noted for US-only layouts where `colon` needs shift; then a "bind verbs directly" subsection with one native example (`focus-workspace-previous`) and one passthrough example (`switch-activity`). Do NOT include `Mod+W` / `Mod+A` / `Mod+Shift+W` (dropped per box 3 — collide with the standard config or target cut verbs). (6) **Dependency contract** — one paragraph: no `niri-ipc` / `jiji-activities` Cargo dep, all compositor/activities interaction via `niri msg` / `jiji-activities` subprocesses; rationale is cross-fork compatibility (runs on upstream niri with a reduced verb set). (7) **Contributing / status** — pre-1.0; design in `docs/design.md`; owned by the unified jiji loop. No screenshots, no GIFs. Landed in `89ac5e6` (was `8039ced` before fixer amend).

**Verification (docs-only sub-phase).** No `cargo test` delta is expected (no `src/` or `tests/` change). The reviewer checks: (a) every verb name / label in the Usage section matches `src/registry.rs::REGISTRY` exactly (13 entries, `list-activities` flagged as menu-hidden); (b) the four capability names match `src/capabilities.rs`; (c) the keybind block contains `Mod+colon` and the `Mod+Space` alternative and does NOT contain `Mod+W` / `Mod+A` / `Mod+Shift+W`; (d) the completion-install instruction is phrased as a post-4.2 forward reference, not a present-tense working command; (e) no chezmoi file is touched.

**Reviewed:** 2026-06-02 (`89ac5e6`, was `8039ced` before the fixer amend). Phase 4.1 — `README.md` (169 lines, docs-only, no `src/` or `tests/` delta) — landed as one commit covering the full seven-section outline ratified at Phase 4.0. Reviewed across comment accuracy, documentation completeness, and content fidelity against the three authoritative sources (`src/registry.rs::REGISTRY`, `src/capabilities.rs`, Phase 4.0 box 3). Post-review fixes squashed into `89ac5e6` (fixer amend from `8039ced`): `README.md` intro section contradiction removed — the NIRI_SOCKET paragraph introduced a `$NIRI_SOCKET` name inconsistency with the capabilities table which uses the unqualified token (fixer corrected the prose); broken relative link to `docs/design.md` replaced with correct path relative to `README.md` at repo root; `--debug` section augmented with a note that menu-hidden verbs (`list-activities`) appear in `--debug` output with "kept" status since they pass the capability gate, distinguishing menu-visibility from capability-miss; category-ordering prose corrected to match the actual stable-by-registration-order-within-category sort contract rather than implying alphabetical or rank ordering. **DD also amended in this commit:** restored missing `## Appendix C: Deferred Suggestions` section header (entries at lines 478–483 were present but the header was absent, a regression from the displacement fixed in `92888e4` that did not survive subsequent DD edits). Post-review fixes: four documentation-accuracy fixes squashed into `89ac5e6` as listed above. Same 55 tests green (18 unit + 3 cli + 34 shims); no `src/` change — `cargo clippy` and `cargo test` baselines unchanged. Proceed to Phase 4.2 (completions infrastructure: `cli::Cli` `value_parser` augmentation via `PossibleValuesParser` lift from `REGISTRY`, `src/completions.rs` module, `Cmd::Completions { shell }` subcommand or `--completions <shell>` global flag, chezmoi `# hash:` bump) with the reviewed base.

---

### Phase 4.2 — Completions infrastructure (one sub-phase, code + test)

Lands the completion surface **re-ratified 2026-06-02 at Phase 4.0 box 2 to option (b)** (per-verb `#[derive(Subcommand)]` enum so clap_complete's fish generator emits verb-name completions — option (a)'s `PossibleValuesParser` lift, landed at `0f9a04a`, was empirically falsified for fish: `jiji-do completions fish | grep -c switch-activity` → `0`) and box 4 (chezmoi `# hash:` bump deferred to this sub-phase). Lands **after** the reviewed Phase 4.1 README, which already forward-references `jiji-do completions fish` (subcommand form) as the post-4.2 install path. **This re-spec supersedes the option-(a) ledger committed at `787aa95`.**

**Central design call — `Cmd` enum is a thin completion-and-parse layer; registry dispatch stays (architect-resolved).** The re-ratified option (b) leaves open whether the `Cmd` enum *replaces* registry dispatch ("registry collapses to metadata only") or sits as a *thin* layer delegating back to `registry::find`. Resolved to the **thin-layer** shape:

- The per-verb `Cmd` variant exists **only** so clap/clap_complete know the verb-name set (fish completion) and so clap rejects unknown verbs at parse time. Each variant maps to its canonical kebab-case name via a `Cmd::name(&self) -> &'static str` method; dispatch then goes through `registry::find(name)` exactly as today (`(verb.dispatch)(&snapshot, verb_arg)`).
- **Why not collapse the registry to metadata only:** the menu path (`registry::enabled_for_menu`, labels, `Category`, capability filtering, `menu_visible`, the dispatch fn pointer) reads the registry today and **must** keep working. The registry remains the single source of truth for everything except the parse/completion surface. Collapsing dispatch into the enum would duplicate capability sets and dispatch wiring into `cli.rs` and orphan `menu_visible` / `Category`, a much larger and riskier diff for zero completion benefit (the enum only needs the variant *names*, which it gets for free from clap-derive's CamelCase→kebab conversion).
- **Capability gating preserved per verb:** the exit-69 path stays exactly where it is in `main.rs` — after `registry::find`, `if !verb.is_enabled(caps) { return Err(DoError::MissingCapability(...)) }`. The enum carries no capability data.

**Source-of-truth sync discipline (load-bearing — registry-as-single-source-of-truth invariant).** Option (b) inherently introduces a second verb list (enum variants vs. `REGISTRY`). A loud parity test mirrors the retired `names_mirrors_registry` idea but at the enum level: **every `Cmd` verb variant maps to a registry verb and every registry verb has a `Cmd` variant**, so a desync fails the test suite rather than silently dropping a verb from completion or from dispatch. See the `cmd_variants_match_registry` test below.

**Three architecturally-resolvable decisions baked in by the architect:**

1. **Subcommand form for `completions`, not global flag.** Phase 4.1's already-reviewed `README.md` (`89ac5e6`) documents the install path verbatim as `jiji-do completions fish`; the global-flag alternative would force a README amendment. The subcommand form is also the `clap_complete` idiom and mirrors `jiji-activities completions <shell>`. (Unchanged from the option-(a) ledger.)

2. **`--debug` stays a `global = true` flag; `cmd` is `Option<Cmd>` so a bare `jiji-do` (no subcommand) opens the menu.** This is the one genuinely fiddly clap-shape interaction. With a `#[derive(Subcommand)]` enum, clap defaults to *requiring* a subcommand. jiji-do must still support three no-subcommand invocations: `jiji-do` (menu), `jiji-do --debug` (diagnostic). So `cmd: Option<Cmd>` (not mandatory), and `--debug` is `#[arg(long, global = true)]` on `Cli` so it parses both bare and alongside any verb subcommand. `jiji-activities` makes `cmd` mandatory (`pub cmd: Cmd`) — jiji-do deliberately diverges here because its no-arg invocation is the menu, the launcher's primary entry point.

3. **Unknown verb → clap parse error (exit 2); the `registry::find` belt-and-suspenders stays.** With a real `Subcommand` enum, an unknown verb (`definitely-not-a-verb`) is rejected by clap at parse time → exit 2, *before* `run` executes — identical to the option-(a) outcome, so the `invalid_verb_exits_2` test survives unchanged and `missing_socket_exits_69` must still be re-pinned with a **valid** verb name (`switch-activity`) run without `$NIRI_SOCKET`. The `registry::find(name).ok_or_else(|| … "unknown verb")` arm in `main.rs` is now effectively unreachable for the dispatch path (clap gates it, and the parity test guarantees enum↔registry coverage) but is **kept as defense-in-depth** against a future enum/registry drift the parity test would also catch — note this in an inline comment. Do not delete it: deleting would turn a hypothetical drift into a silent wrong-dispatch.

**Verb-string surface is byte-for-byte preserved.** clap-derive converts CamelCase variants to kebab-case subcommand names automatically (`SwitchActivity` → `switch-activity`), exactly as in `jiji-activities`. Every existing `tests/shims.rs` / `tests/cli.rs` invocation (`jiji-do switch-activity`, `jiji-do create-activity foo`, `jiji-do remove-activity ""`, `jiji-do --debug`) parses to the same surface. **No shim test changes are expected** beyond the two `tests/cli.rs` updates below — this is the load-bearing assertion the reviewer should verify (all 34 shims green, unchanged).

**Variant shapes (verb_arg mapping):** the two verbs that consume `verb_arg` (`create-activity <name>`, `remove-activity <name>`) get a variant carrying `verb_arg: Option<String>` (positional, optional — both have a no-arg picker path per their shim tests at `tests/shims.rs:909/950` and `1164/1212`); all other verbs are **unit variants** (no positional). The dispatch site threads `verb_arg.as_deref()` into `(verb.dispatch)(&snapshot, verb_arg)` exactly as the current `args.verb_arg.as_deref()` does — so `verbs/create_activity.rs` and `verbs/remove_activity.rs` are untouched.

- [x] `Cargo.toml` — add `clap_complete = "4"` to `[dependencies]`. (`clap` already present at `version = "4"`, `features = ["derive"]`.) No version pin beyond major; mirror `jiji-activities`. (Carries over verbatim from `0f9a04a`.) Landed in `81de258`.

- [x] `src/cli.rs` — replace the option-(a) `verb`/`verb_arg`/`PossibleValuesParser` shape with the per-verb subcommand enum:
  - `Cli { #[arg(long, global = true)] debug: bool, #[command(subcommand)] cmd: Option<Cmd> }`. Drop the `verb: Option<String>` and `verb_arg: Option<String>` positionals and the `verb_value_parser()` helper — they are subsumed by the enum.
  - `#[derive(Subcommand, Debug)] enum Cmd { … }` with **one variant per `REGISTRY` verb plus `Completions`**:
    - 11 unit variants: `SwitchWorkspace`, `FocusWorkspacePrevious`, `ToggleDebugTint`, `SwitchActivity`, `SwitchActivityPrevious`, `MoveWindowToActivity`, `MoveWindowHere`, `MoveWorkspaceToActivity`, `AssignWorkspace`, `SaveActivity`, `ListActivities`.
    - 2 name-bearing variants: `CreateActivity { verb_arg: Option<String> }`, `RemoveActivity { verb_arg: Option<String> }` (positional name field; field name `verb_arg` keeps the threading obvious — or `name`, implementer's call, but the positional must be `Option<String>` to preserve the no-arg picker path).
    - `Completions { shell: clap_complete::Shell }`.
    - Confirm the CamelCase→kebab auto-conversion produces the exact registry names (it does for all 13: `SwitchWorkspace`→`switch-workspace`, etc.). Any variant whose kebab form would diverge from its registry `name` needs `#[command(name = "…")]` — none should, but verify against `REGISTRY` at implementation time (this is what the parity test pins).
  - Add an `impl Cmd { fn verb_name(&self) -> Option<&'static str> }` (or a free fn) returning the canonical registry name for the 13 verb variants and `None` for `Completions`. Used by `main.rs` to bridge enum→`registry::find`. (Alternatively, match in `main.rs` directly; a `verb_name` accessor keeps the bridge in one place and is what the parity test exercises.) Landed in `81de258`.

- [x] `src/registry.rs` — **remove** the option-(a) `NAMES: LazyLock<Vec<&'static str>>` and its `names_mirrors_registry` test (they existed only to feed the `PossibleValuesParser`, now gone). `use std::sync::LazyLock;` becomes unused — drop it. The enum is now the parse/completion source; the parity test (in `cli.rs` or `registry.rs`) replaces `names_mirrors_registry`. Landed in `81de258`.

- [x] `src/cli.rs` (or `src/registry.rs`) — **`cmd_variants_match_registry` parity test** (replaces the retired `names_mirrors_registry`). Pin enum↔registry sync both directions:
  - Build the set of canonical verb names the enum knows (iterate a `const`/match listing every verb variant's `verb_name`, or enumerate explicitly) and assert it equals `REGISTRY.iter().map(|v| v.name).collect()` as a set, **and** assert the counts match (13 verbs). A new `REGISTRY` verb without a `Cmd` variant (or vice versa) fails loudly. Comment this as the load-bearing source-of-truth-sync guard for the option-(b) two-list hazard. Landed in `81de258`.

- [x] `src/completions.rs` — carries over from `0f9a04a` essentially unchanged (it already generates from `Cli::command()` via `clap_complete::generate`; the enum reshape is what makes its fish output now contain verb names). Module-level rustdoc and the `completions_are_non_empty_and_bash_contains_known_verb` unit test are **updated**: the fish assertion is tightened from "non-empty" to "contains `switch-activity`" because option (b) fixes exactly the fish gap. The comment that previously explained "fish's generator does not enumerate positional possible-values inline" updated — under the subcommand enum, fish **does** emit verb-name completions. Fish-contains-verb pinned explicitly as the regression guard for the whole re-spec. Landed in `81de258`.

- [x] `src/main.rs` — rewrite the dispatch in `run`:
  - Keep the `Completions` early return: `if let Some(cli::Cmd::Completions { shell }) = &args.cmd { return completions::run(*shell); }` before the capability probe / socket gate.
  - After the socket gate + snapshot capture, replace `match args.verb { Some(name) => … None => menu }` with `match args.cmd`:
    - `Some(verb_cmd)` (any non-`Completions` variant) → resolve its canonical name via `verb_cmd.verb_name()`, extract `verb_arg` from the name-bearing variants (`CreateActivity { verb_arg } | RemoveActivity { verb_arg }`), `registry::find(name)`, the **unchanged** capability gate (`is_enabled` → exit 69), then `(verb.dispatch)(&snapshot, verb_arg.as_deref())`.
    - `None` → the menu path, unchanged (`registry::enabled_for_menu`, fuzzel gate, `render_menu`).
  - Keep the inline comment on the `registry::find(name).ok_or_else(…)` arm noting clap is now the primary unknown-verb gate and the parity test guards drift; keep the arm (decision 3). `print_debug` is unchanged (iterates `REGISTRY` directly, no `verb`/`cmd` dependency).
  - `mod completions;` already in the module list from `0f9a04a` — no change. Landed in `81de258`.

- [x] `tests/cli.rs` — the option-(a) tests mostly carry over (the surface contract is identical: invalid verb → exit 2, valid verb no socket → exit 69, `completions fish`/`bash` emit content). Verified under the enum shape:
  - `missing_socket_exits_69` — already re-pinned at `0f9a04a` with a **valid** verb name (`switch-activity`) and `env_remove("NIRI_SOCKET")` → exit 69. Holds (clap parses `switch-activity` fine, socket gate fires). Kept as-is.
  - `invalid_verb_exits_2` — `definitely-not-a-verb` → exit 2 at clap parse time. Under the `Subcommand` enum this is clap's unknown-subcommand error (still exit 2). Holds.
  - `completions_fish_exits_zero_with_content` — unchanged.
  - `completions_bash_contains_registered_verbs` — unchanged.
  - `completions_fish_contains_registered_verbs` (added) — `jiji-do completions fish` stdout contains `switch-activity`. Integration-level proof that option (b) fixed the fish gap option (a) could not. Landed in `81de258`.

**Out of scope (this sub-phase):**
- Dynamic `verb_arg` completion (shelling to `niri msg --json activities` at tab time for `remove-activity`, freeform for `create-activity`) — deferred to a Stage-5+ amendment. (Unchanged from option (a).)
- Collapsing the registry to metadata-only / moving dispatch into the `Cmd` enum — explicitly rejected (see central design call); the registry stays the single source of truth for labels, categories, capability sets, `menu_visible`, and dispatch fn pointers.
- `--help` "list categories" grouping — not pursued; clap-derive `--help` already lists the subcommand surface (and now lists all verbs by name, an incidental improvement over option (a)).

**chezmoi wiring — flagged as a SEPARATE human/chezmoi-repo task, NOT a `repos/jiji-do` deliverable.** Box 4 ratified "chezmoi `# hash:` bump deferred to 4.2." The bump and any new completion-generation block live in `~/.local/share/chezmoi/run_onchange_install-packages.sh.tmpl` — a **separate git repo** outside the loop's `dd_commit_repo`. jiji-do is NOT yet wired into that run_onchange script (only the compositor and jiji-activities are). Wiring jiji-do in requires: (a) adding a `cargo install --path … && jiji-do completions fish > ~/.config/fish/completions/jiji-do.fish` block (or equivalent) to the template, (b) bumping `# hash:` to force a re-fire, (c) committing in the chezmoi repo. The loop commits only land in `repos/jiji-do`; this chezmoi work is surfaced for the human to perform (or to request as an explicit follow-up) and is intentionally excluded from the implementer's scope to keep the sub-phase self-contained within the registered `dd_commit_repo`.

**Verification.** `cargo +nightly fmt --all`; `cargo clippy --all --all-targets` zero-warning; `cargo test`. Test arithmetic relative to the **`0f9a04a` baseline** (the landed option-(a) commit, which already added the `completions.rs` unit test + `invalid_verb_exits_2` + the two `completions_*` cli tests on top of the 55-test Phase 4.1 base):
- **Unit:** drop `registry::names_mirrors_registry` (NAMES retired), add `cmd_variants_match_registry` (parity) → net 0 in the registry bucket. The `completions.rs` unit test stays (carried over; assertion tightened to fish-contains-verb).
- **cli:** all four `0f9a04a` cli tests carry over; optionally add `completions_fish_contains_registered_verbs` (+1, recommended).
- **shims:** **unchanged — all 34 must stay green untouched** (the kebab-case verb-string surface is byte-for-byte identical under the enum). This is the load-bearing regression assertion.

The exact total depends on whether `0f9a04a` is amended (preferred) vs. reset, and whether the optional fish-contains cli test is added. The load-bearing assertions: (a) no shim test changes; (b) `fish` completions now contain verb names (the whole point of the re-spec); (c) `cmd_variants_match_registry` is present and green. Verify exact bucket counts at implementation time and record them in the Reviewed: block.

**Commit shape (re-spec of a landed full-loop commit).** Option (a) landed at `0f9a04a` (unpushed). The `clap_complete` dep, `src/completions.rs`, the `Completions` early-return scaffold in `main.rs`, and the `completions_*` tests all carry over; what gets *replaced* is the `verb`/`verb_arg` positionals + `PossibleValuesParser` + `registry::NAMES` (→ the `Cmd` verb-variant enum + `verb_name` bridge + `cmd_variants_match_registry`). The implementer should **`git commit --amend` `0f9a04a`** into the final option-(b) commit (preferred — keeps Phase 4.2 as one commit; the commit is unpushed so the amend is clean and rewrites no published history), not a follow-on commit. The commit subject should describe the verb-subcommand-enum shape, not reference option (a)/(b) or the DD (commit-hook discipline). A `git reset --soft 787aa95` then re-commit is an acceptable alternative if the amend diff is awkward to stage, but the amend is cleaner.

**Reviewed:** 2026-06-02 (`81de258`, amended from `ae21174` after the fixer squashed one comment-accuracy fix). Phase 4.2 — completions infrastructure via re-ratified option (b), the per-verb `#[derive(Subcommand)]` enum — landed as one code-bearing commit; `d8eae17` carries the DD re-ratification (architect-committed, not re-authored here). Reviewed across code quality, silent-failure surface, comment accuracy, test coverage, and type design. **Finding worth surfacing (clap_complete fish generator does not emit positional PossibleValuesParser values, `src/cli.rs`):** option (a)'s `PossibleValuesParser` lift on `verb: Option<String>` delivers zero fish completions — the fish generator enumerates real subcommand variants only; bash/zsh do emit positional possible-values but fish does not. Empirical proof: `jiji-do completions fish | grep -c switch-activity` → `0` at `0f9a04a`; → `21` at `81de258`. This is the decision record for why the design pivoted mid-sub-phase to the subcommand enum. The regression guard is `tests/cli.rs::completions_fish_contains_registered_verbs`. **Finding worth surfacing (thin-layer enum design, `src/cli.rs`):** the `Cmd` enum is a completion-and-parse layer only — each variant maps to a registry name via `Cmd::verb_name()`; dispatch still goes through `registry::find(name)`. The "registry collapses to metadata" path of option (b)'s original analysis was explicitly rejected: labels, `Category`, capability sets, `menu_visible`, and the dispatch fn pointer stay in the registry. The fixer amend (`ae21174` → `81de258`) removed a comment in the parity test claiming `cmd_variants_match_registry` provides "compile-time" guarantees — the guarantee is test-time (a runtime assertion), not a compiler guarantee; the comment was overstating. **Finding worth surfacing (deliberate exit-2 tightening, `tests/cli.rs`):** with a real `Subcommand` enum, clap rejects unknown verbs at parse time (exit 2, before `run` executes). Capability-miss on a valid verb stays exit 69. A stray positional on a unit variant is also exit-2-rejected — the enum makes ignored-positional misuse structurally impossible, eliminating the `verb_arg: Option<String>` silent-ignore hazard. The `registry::find` belt-and-suspenders arm is retained as defense-in-depth; its `ok_or_else` is now effectively unreachable from any enum-legal parse, but the parity test keeps it honest. Post-review fixes squashed into `81de258` (from `ae21174`): `src/registry.rs::cmd_variants_match_registry` — parity test comment corrected from "compile-time guarantee" to accurate "test-time set-equality + count assertion." All 34 shims green, unchanged (byte-identical verb-string surface under the enum — the load-bearing regression assertion). Final test buckets: 20 unit + 7 cli + 34 shims = 61 (up from Phase 4.1's 55: +1 unit `cmd_variants_match_registry`, −1 unit `names_mirrors_registry`, +1 unit `completions_are_non_empty_and_bash_contains_known_verb` tightened fish assertion, net 0 unit change from 18; +1 cli `completions_fish_contains_registered_verbs`; 34 shims unchanged; exact: 20 unit + 7 cli + 34 shims). `cargo clippy --all --all-targets` zero warnings; `cargo +nightly fmt --all` clean. Stage 4 complete; jiji-do is feature-complete for v0.1.0. Remaining items are human/external: (a) chezmoi wiring (add `jiji-do completions fish` generation block to `run_onchange_install-packages.sh.tmpl` — separate chezmoi repo task, not a loop deliverable); (b) Phase D (push `gajdusek/jiji` + tools to GitHub, which unblocks the v0.1.0 tag and any git+rev pins). Proceed to the chezmoi wiring follow-up and Phase D with the reviewed base.

---

## Stage 5 — direct activity targeting + dynamic activity-name completion

Approved design (2026-06-03): `docs/superpowers/specs/2026-06-03-jiji-do-dynamic-activity-completion-design.md` (workspace repo). No ratification gate — the scope was decided and approved in brainstorm; the boxes below are implementable directly.

**What and why.** jiji-do's activity-targeting verbs are picker-only; there is no way to name the target on the CLI and therefore no tab-completion of activity names. `jiji-activities` (the delegate) already accepts an optional name positional with picker-fallback on `switch` / `move-window` / `move-workspace` / `remove` / `save`, and already augments its fish completion from `jiji-activities list --format=name`. jiji-do's verb `run()` functions already receive `arg: Option<&str>` (currently ignored). So Stage 5 forwards the name jiji-do already receives and mirrors the completion pattern jiji-activities already proves.

**Baked decisions.** Completion set = 5 verbs (`switch-activity`, `move-window-to-activity`, `move-workspace-to-activity`, `remove-activity`, `save-activity`). `save-activity` gains an optional override name (omit → derive focused name, current behavior). Hard exclusions (mirror jiji-activities, with tests): `assign-workspace` (no name positional in the delegate — multi-select picker only) and `create-activity` (freeform new name). Every verb's omit-path is unchanged — strictly additive.

**Dependency contract.** Unchanged: the dynamic completion shells `jiji-activities list --format=name` at tab time (subprocess, not a Cargo dep). `tests/cli.rs::no_forbidden_dependencies` stays green.

### Phase 5.1 — forward the activity name (verb surface)

- [x] `src/cli.rs` — change four `Cmd` variants from unit to `{ verb_arg: Option<String> }`: `SwitchActivity`, `MoveWindowToActivity`, `MoveWorkspaceToActivity`, `SaveActivity` (`RemoveActivity` / `CreateActivity` already carry it). Update `Cmd::verb_arg()` to return the inner `Option<&str>` for these four. `Cmd::verb_name()` unchanged. Landed in `1fb1176`.
- [x] `src/cli.rs` (or wherever it lives) — update the `cmd_variants_match_registry` parity test to construct the four newly-name-bearing variants with `verb_arg: None`. Test must stay green (set + count equality, both directions). Landed in `1fb1176`.
- [x] `src/verbs/switch_activity.rs` — stop ignoring `arg`; `Some(name)` → `jiji-activities switch <name>`, `None` → `jiji-activities switch` (its picker, current behavior). Landed in `1fb1176`.
- [x] `src/verbs/move_window_to_activity.rs` — `Some(name)` → `jiji-activities move-window <name> --window=<id>`, `None` → `jiji-activities move-window --window=<id>` (current). Preserve the snapshot-sourced `--window` flag and the no-focused-window bail. Landed in `1fb1176`.
- [x] `src/verbs/move_workspace_to_activity.rs` — `Some(name)` → `jiji-activities move-workspace <name> --workspace=<id>`, `None` → `jiji-activities move-workspace --workspace=<id>` (current). Preserve the `--workspace` flag and the no-focused-workspace bail. Landed in `1fb1176`.
- [x] `src/verbs/save_activity.rs` — `Some(name)` → `jiji-activities save <name>` (save-as), `None` → `jiji-activities save <focused-name>` (derive from snapshot, current behavior + the no-focused-activity bail). Landed in `1fb1176`.
- [x] `tests/shims.rs` — per the four newly-forwarding verbs: a positive pin (name supplied → recorded `jiji-activities` argv contains the name in the correct position, plus the preserved `--window`/`--workspace` flag where applicable) and an omit-path pin (no name → argv byte-identical to today). `save-activity` omit-path still derives the focused name. Other verbs' existing shims must stay green. Landed in `1fb1176`.

**Reviewed:** 2026-06-03 (`1fb1176`, was `888076f` before the fixer amend). Phase 5.1 — four `Cmd` variants widened from unit to `{ verb_arg: Option<String> }` (`SwitchActivity`, `MoveWindowToActivity`, `MoveWorkspaceToActivity`, `SaveActivity`), verb dispatch updated to forward the supplied name, eight new shim tests — landed as one commit; fixer squashed empty-string normalization into `888076f` → `1fb1176`. Reviewed across code quality, silent-failure surface, comment accuracy, test coverage, and type design. **Finding worth surfacing (name-before-flag ordering for move verbs, `src/verbs/move_window_to_activity.rs`, `src/verbs/move_workspace_to_activity.rs`):** the delegate's subcommand signature for `move-window` and `move-workspace` takes the activity name as a positional *before* the `--window`/`--workspace` flag — the name-before-flag ordering is therefore mandatory, not a style call. The commit places the name in the correct position (`jiji-activities move-window <name> --window=<id>`) and the positive shim tests pin the full argv string, so a flag-order regression would fail loudly. **Finding worth surfacing (`save-activity` bail asymmetry, `src/verbs/save_activity.rs`):** the no-focused-activity bail applies only to the omit/empty-name path (where the verb must derive the name from the snapshot); the name-supplied path bypasses that bail entirely and shells directly to `jiji-activities save <name>`. This is the correct asymmetry — "save as a specific named activity" does not require knowing the currently focused activity — but the distinction is subtle enough that a future maintainer might wrongly add the bail to both branches. The positive shim test for the name-supplied case (with no focused-activity in the snapshot fixture) implicitly pins this asymmetry. **Finding worth surfacing (empty-string normalization added in fixer pass, `src/verbs/*.rs`):** the pre-amend commit (`888076f`) forwarded `arg` directly without normalizing empty/whitespace strings; an explicitly empty positional (`jiji-do switch-activity ""`) would have dispatched `jiji-activities switch ""` rather than routing to the picker. The fixer squashed `arg.map(str::trim).filter(|s| !s.is_empty())` normalization into all four forwarding verbs, matching the precedent from `create_activity.rs` / `remove_activity.rs`. Post-review fixes squashed into `1fb1176`: empty-string normalization (`arg.map(str::trim).filter(|s| !s.is_empty())`) added to all four forwarding verbs (`switch_activity.rs`, `move_window_to_activity.rs`, `move_workspace_to_activity.rs`, `save_activity.rs`). Test count 61 → 73 (20 unit + 7 cli + 46 shims; +12 vs. Phase 4.2 baseline of 61: 8 new forwarding shims spec'd + 4 added in review for empty-string normalization = 12 new shims, 46 total). Same 73 tests green; `cargo clippy --all --all-targets` zero warnings; `cargo +nightly fmt --all` clean. Proceed to Phase 5.2 (dynamic fish completion: `FISH_SINGLE_ARG_VERBS`, `FISH_NAMES_CMD`, `emit_fish_dynamic`, `no_positional_yet` helper) with the reviewed base.

### Phase 5.2 — dynamic fish completion

- [x] `src/completions.rs` — mirror `repos/jiji-activities/src/completions.rs`: add `FISH_SINGLE_ARG_VERBS = ["switch-activity", "move-window-to-activity", "move-workspace-to-activity", "remove-activity", "save-activity"]`, `FISH_NAMES_CMD = "jiji-activities list --format=name 2>/dev/null"`, `emit_fish_dynamic` (one `complete -c jiji-do -n "__fish_jiji_do_using_subcommand <verb>; and <no-positional-yet>" -f -a "({FISH_NAMES_CMD})"` per verb), and the `no_positional_yet` helper.
- [x] `src/completions.rs` — add a global `complete -c jiji-do -f` guard so argument-less verbs stop offering file paths after the verb (folds in the file-fallback polish). Verify it composes with the per-verb dynamic `-a` lines (global `-f` suppresses file fallback; conditional `-a` still adds activity names). `run(Fish)` emits: clap base → `no_positional_yet` helper → `emit_fish_dynamic` → global `-f`. Non-fish shells: clap base only.
- [x] `src/completions.rs` tests — mirror jiji-activities' `completions::tests`: dynamic line emitted per `FISH_SINGLE_ARG_VERBS` entry; **no** line for `assign-workspace` or `create-activity` (the hard exclusions); candidates use `jiji-activities list --format=name`; `no_positional_yet` helper defined. Keep `completions_fish_contains_registered_verbs` and `cmd_variants_match_registry` green.

**Reviewed:** 2026-06-03 (`940d391`). Phase 5.2 — dynamic fish activity-name completion for `jiji-do` — landed as one clean first-pass commit (no fixer round). Reviewed across code quality, silent-failure surface, comment accuracy, test coverage, and type design. **Finding worth surfacing (faithful mirror of `jiji-activities/src/completions.rs`, `src/completions.rs`):** the augmentation closely follows the proven pattern from `repos/jiji-activities/src/completions.rs` — same `FISH_SINGLE_ARG_VERBS` / `FISH_NAMES_CMD` constants, same `emit_fish_dynamic` + `emit_no_positional_yet_helper` split, same global `-f` guard emitted last, same `run(Fish)` emission order. The structural parallel means any future change to the `jiji-activities` pattern has a clear analogue to mirror here. **Finding worth surfacing (`__fish_seen_subcommand_from` structurally avoided, `src/completions.rs`):** the position-gated condition uses clap_complete's own `__fish_jiji_do_using_subcommand <verb>` helper combined with `__jiji_do_no_positional_yet` (a token-count helper emitted by this module). `__fish_seen_subcommand_from` is never used — that builtin fires anywhere the verb token appears in the commandline, not only at the first positional position, which would trigger completions mid-flag-value. The token-count approach (`commandline -opc | count` = 2) constrains candidates to the exact first-positional slot, consistent with the `[[feedback_completions_position_aware_condition_rule]]` invariant. **Finding worth surfacing (global `-f` guard composition, `src/completions.rs`):** the global `complete -c jiji-do -f` guard is emitted *last*, after all conditional `-a` lines. Fish processes completions in emission order: the global `-f` suppresses file-system fallback unconditionally, but conditional `-a` lines already registered for a given context still deliver their candidates. The emission order is therefore load-bearing — placing the global guard before the per-verb lines would not change behavior (fish `-a` additions are additive over `-f`), but the current order documents intent clearly and matches the `jiji-activities` precedent. **Finding worth surfacing (hard exclusions pinned by negative-space tests, `src/completions.rs::tests`):** `assign-workspace` (unit variant, no positional) and `create-activity` (freeform new name, not an existing activity name) are absent from `FISH_SINGLE_ARG_VERBS` and their absence is tested directly with `assert!(!output.contains("assign-workspace"))` / `assert!(!output.contains("create-activity"))` style negative assertions. Removes the silent-drift risk where a future refactor moves either verb into the dynamic set without review. Post-review fixes squashed into `940d391`: none needed in the commit itself. Test count 73 → 79 (26 unit + 7 cli + 46 shims; +6 unit vs. Phase 5.1 baseline of 73: 6 new unit tests in `src/completions.rs::tests` for dynamic line presence, hard exclusions, `FISH_NAMES_CMD` content, helper definition, and global-guard emission; shims and cli buckets unchanged). Same 79 tests green; `cargo clippy --all --all-targets` zero warnings; `cargo +nightly fmt --all` clean. Stage 5 complete; jiji-do is dynamic-completion feature-complete. Remaining items are human/external: (a) chezmoi follow-up — reinstall jiji-do (`cargo install --path .` in `repos/jiji-do/`) and bump `# hash:` 17 → 18 in chezmoi `run_onchange_install-packages.sh.tmpl` so the regenerated `jiji-do completions fish` carries the new dynamic augmentation (the generation block is already wired at chezmoi `9adffaf`); (b) Phase D (push `gajdusek/jiji` + tools to GitHub, which unblocks the v0.1.0 tag and git+rev pins). Proceed to the chezmoi follow-up (actionable now) and Phase D with the reviewed base.

### Stage 5 post-landing follow-up (human / chezmoi repo — not a loop deliverable)

CLI surface change (4 verbs gain a positional) → reinstall jiji-do (`cargo install --path .`) and bump `# hash:` in chezmoi `run_onchange_install-packages.sh.tmpl` (17 → 18) so the completion regenerates. The `jiji-do completions fish` block is already wired (chezmoi `9adffaf`); only the hash bump + reinstall are needed, no script edit.

## Stage 6 — post-v0.1.0 quality hardening

Two pre-blessed Appendix C deferrals, user-approved 2026-06-03 as a single small landing unit. **No ratification gate** — these were already reviewer-rated and parked at Stage 1 review (`a0eaccc`); the user has simply un-parked exactly two of them. Both touch `src/snapshot.rs` / `src/niri.rs` / their tests, are strictly additive or purely deletive, and land as **one commit**. The two corresponding Appendix C entries are annotated *promoted to Stage 6* so they are not double-tracked.

**Scope note on file attribution.** Appendix C's second entry attributes `parse_workspace_choices` to `src/snapshot.rs`; the function actually lives in `src/niri.rs` (`WorkspaceChoice` picker labels). The label-fallback format is `"<output> #<id>"` (with a `#`), so the `"? #id"` shorthand in the Appendix entry refers to the both-null case (`output: null` → `"?"`, hence `"? #<id>"`).

### Phase 6.1 — snapshot + parse hardening

- [x] `src/snapshot.rs` — drop `#[derive(Default)]` from `Snapshot` (line 31, `#[derive(Debug, Default, PartialEq, Eq)]` → `#[derive(Debug, PartialEq, Eq)]`). No caller constructs `Snapshot::default()` (grep-verified across `src/` and `tests/` at authoring time — zero hits); removing it closes the empty-`Snapshot` construction bypass of the `from_json` / `capture` seam. If the implementer's own grep surfaces a `Snapshot::default()` use, that call site migrates to `from_json` / an explicit field literal in the same commit. Landed in `2244ef3`.
- [x] `src/niri.rs` — add three unit tests for `parse_workspace_choices` covering the unexercised cases (the existing `parse_uses_name_then_falls_back` covers only `name: null` + non-null `output`): (1) **both-null fallback** — a row with `name: null, output: null` yields label `"? #<id>"`; (2) **empty array** — `"[]"` yields `Ok(vec![])`; (3) **malformed JSON** — a non-array / syntactically broken string yields `Err` (assert `.is_err()`, do not pin the serde message). Place them in the existing `#[cfg(test)] mod tests` block alongside `parse_uses_name_then_falls_back`. Landed in `2244ef3`.

**Exit criteria.** `cargo test` green at one higher unit count (+3 unit tests, no other bucket change); `cargo clippy --all --all-targets` zero warnings; `cargo +nightly fmt --all` clean; no `Snapshot::default()` reference remains anywhere in the tree.

**Reviewed:** 2026-06-03 (`2244ef3`). Phase 6.1 — `#[derive(Default)]` dropped from `Snapshot` and three `parse_workspace_choices` edge-case unit tests added — landed as one commit (clean first-pass review, no fixer round). Reviewed across code quality, silent-failure surface, comment accuracy, and test coverage. **Finding worth surfacing (`Default` removal, construction-seam tightening, `src/snapshot.rs`):** dropping the derive closes the `Snapshot::default()` bypass of the `from_json` / `capture` seam without any call-site migration cost — grep confirmed zero consumers across `src/` and `tests/` at both authoring and review time, so the change is purely deletive with no behavioral delta. `PartialEq`, `Eq`, and `Debug` are preserved (test-equality and debug-print paths remain live). **Finding worth surfacing (both-null label format, `src/niri.rs`):** the Appendix C entry used the shorthand `"? #id"` for the both-null case; the actual label produced is `"? #<id>"` (with a `#` prefix before the numeric id), consistent with the non-null output path that emits `"<output> #<id>"`. The test pins the exact format string with a specific id, so the `#` is now a contract rather than informal prose. Post-review fixes squashed into `2244ef3`: none needed in the commit itself. Test count 79 → 82 (29 unit + 7 cli + 46 shims; +3 unit vs. Phase 5.2 baseline of 79: `parse_workspace_choices_both_null_fallback`, `parse_workspace_choices_empty_array`, `parse_workspace_choices_malformed_json`; cli and shims buckets unchanged). `cargo clippy --all --all-targets` zero warnings; `cargo +nightly fmt --all` clean. Closes the two un-parked Appendix C deferrals (entries 3 and 4 — `Default` derive and `parse_workspace_choices` edge cases); entries 1, 2, and 5 (`DoError::MissingCapability` typing, `FocusedWorkspace` nesting, conditional-FUZZEL requires) remain parked in Appendix C. Proceed to Stage 6 complete / the command-expansion design brainstorm (in progress 2026-06-03) with the reviewed base.

---

## Stage 7 — command expansion: native IPC-wrapping verbs

Curates ~14 non-keybound jiji IPC actions into new verbs, per the approved design at `~/projects/desktop/de/jiji/docs/superpowers/specs/2026-06-03-jiji-do-command-expansion-design.md` (brainstorm-approved 2026-06-03). **No ratification gate** — scope, curation, and the two UX decisions were settled in the brainstorm; the boxes below are implementable directly.

**Curation grounding.** Every verb here cleared both curation gates (`CLAUDE.md`): non-keybound in the standard config, and launcher-suitable (picker-based, discovery, or infrequent). Candidates found bound and dropped: `show-hotkey-overlay`, `switch-layout`, `screenshot`, `toggle-column-tabbed-display`. Cut as niche: debug toggles, window-urgency, dynamic-cast, screen-transition. **Deferred (no backing action):** the `save-*` family.

**Two ratified decisions.** (1) `quit` and `power-off-monitors` are kept *despite* being keybound (explicit user request); both are menu-visible **with a fuzzel confirm** before dispatch. `quit` uses `action quit --skip-confirmation` (the fuzzel confirm replaces niri's). (2) `rename-activity` is a **passthrough** to the new jiji-activities `rename` verb (its Phase 3.12), not a native action call — keeping all activity ops behind the delegate. Stage 7 Phase 7.6 therefore **depends on** jiji-activities Phase 3.12.

**Dependency contract preserved.** All dispatch is `niri msg` / `jiji-activities` subprocess. `wl-copy` / `notify-send` (pick-\* routing) are best-effort subprocesses, not Cargo deps, not capability-gated. `tests/cli.rs::no_forbidden_dependencies` stays green. The architect may merge small phases (e.g. 7.2 + 7.3) at land time.

### Phase 7.1 — simple native verbs + `Category::System` + pick-\* routing

- [x] `src/registry.rs` — extend the `Category` enum with `System` (Ord placement last, so system/dangerous actions sort to the menu bottom); confirm `enabled` / `enabled_for_menu` category-grouped stable sort holds with the new variant.
- [x] `src/verbs/reload_config.rs` (new) + register `reload-config` (System; `NIRI_SOCKET`; menu-visible) → `niri msg action load-config-file` (no path → reload default).
- [x] `src/verbs/power_on_monitors.rs` (new) + register `power-on-monitors` (System; `NIRI_SOCKET`) → `niri msg action power-on-monitors`.
- [x] `src/verbs/unset_workspace_name.rs` (new) + register `unset-workspace-name` (Workspace; `NIRI_SOCKET`) → `niri msg action unset-workspace-name` (focused ws — omit the reference, compositor defaults to focused).
- [x] `src/verbs/pick_window.rs` (new) + register `pick-window` (Window; `NIRI_SOCKET`) → `niri msg pick-window`; capture stdout, best-effort `notify-send` a human summary of the picked window; fall back to printing stdout if `notify-send` is absent/fails. A missing notifier must NOT fail the verb (the pick succeeded).
- [x] `src/verbs/pick_color.rs` (new) + register `pick-color` (System; `NIRI_SOCKET`) → `niri msg pick-color`; capture stdout, best-effort `wl-copy` the returned color + `notify-send`; fall back to stdout. Soft deps, not capability-gated.
- [x] `src/cli.rs` — five new unit `Cmd` variants; update `cmd_variants_match_registry` (count +5).
- [x] `tests/shims.rs` — per verb an exact-argv pin; pick-\* routing tests (shim `wl-copy` / `notify-send`, assert the captured stdout is routed to them); a notifier-missing test asserting the verb still exits 0 and falls back to stdout. Account for the capability-probe invocation in argv assertions.

**Reviewed:** 2026-06-03 (`c413e97`, history: `dfb07f1` → `fc4fc2c` → `c413e97` through two fixer/review rounds). Phase 7.1 — `Category::System` enum variant + five new native verbs (`reload-config`, `power-on-monitors`, `unset-workspace-name`, `pick-window`, `pick-color`) + `proc::run_best_effort` soft-dep seam — landed as one squashed commit. Reviewed across code quality, silent-failure surface, comment accuracy, test coverage, and type design. **Finding worth surfacing (`Category::System` Ord placement, `src/registry.rs`):** `System` is declared last in the enum, not via explicit discriminants, so the category-grouped stable sort delivers `System` verbs to the menu bottom by Rust's `#[derive(Ord)]` declaration-order semantics. The existing category-ordering unit tests were updated to assert the new placement; a regression in declaration order would flip the sort without a compile error but would fail the assertion, making the ordering a tested contract rather than a convention. **Finding worth surfacing (pick-\* asymmetric failure contract, `src/verbs/pick_color.rs`, `src/verbs/pick_window.rs`, `src/proc.rs`):** the pick verbs enforce two distinct failure modes: the pick itself fails loud (propagates `Err` on compositor error or user cancel), while routing the captured value to the clipboard/notifier fails silent (`proc::run_best_effort` returns `false` on absent binary or non-zero exit, never `Err`). This asymmetry is correct — the value is produced by a modal compositor picker; losing it to a routing failure is unacceptable. `run_best_effort` is the seam that enforces the soft-dep half of the contract. **Finding worth surfacing (pick-color clipboard-miss → stdout fallback decoupled from notify, `src/verbs/pick_color.rs`):** the initial fixer pass used an `if !copied && !notified` guard for the stdout fallback, which introduced a silent-failure hole: when `wl-copy` was absent or failed but `notify-send` succeeded, the color was announced ephemerally but was not retrievable (neither clipboard nor stdout). A notification is a transient signal; the clipboard/stdout path is the value's retrievable home. The human ratified a fix: the naive `which("wl-copy")`-based gate was identified as a no-op (since `run_best_effort` already returns `false` on absent binary) and the stdout fallback was decoupled from the notify leg — final rule: `if !copied { print!("{color}") }`. `pick-window` was deliberately left with a single informational sink (`notify-send` → stdout fallback, no clipboard step), since window info has no retrievability asymmetry. This is the same silent-failure family as the fuzzel cancel-vs-failure fix (`a0eaccc`): a lesser or ephemeral sink must not suppress the fallback the value's retrievable home needs. **Finding worth surfacing (`proc::run_best_effort` stdin-pipe seam, `src/proc.rs`):** `run_best_effort` accepts an optional stdin `&str` and pipes it via `ChildStdin` write + explicit drop before `wait()` — necessary for `wl-copy` which reads from stdin. The drop-before-wait ordering is load-bearing: not closing stdin before waiting would deadlock on any program that blocks reading until EOF. A unit test covers the stdin path alongside the spawn-error and non-zero-exit edges. Post-review fixes squashed into `c413e97`: pick-color stdout fallback decoupled from notify leg (`src/verbs/pick_color.rs` — `if !copied { print!("{color}") }` replacing the `if !copied && !notified` guard); `which("wl-copy")`-based no-op gate removed. `notify-send` / `wl-copy` remain non-capability-gated soft deps (Appendix C entry 6 parked). Test count 82 → 98 (34 unit + 7 cli + 57 shims; +16 vs. Phase 6.1 baseline of 82: +5 unit — `run_best_effort` edges including stdin-pipe, `Category` declaration-order assertions; +11 shims — per-verb argv pins, asymmetric-contract both-directions, pick-color partial-failure diagonals; cli bucket unchanged). Same 98 tests green; `cargo clippy --all --all-targets` zero warnings; `cargo +nightly fmt --all` clean. Proceed to Phase 7.2 (`menu::confirm` + confirm-gated `quit` / `power-off-monitors`) with the reviewed base.

### Phase 7.2 — `menu::confirm` + confirm-gated `quit` / `power-off-monitors`

- [ ] `src/menu.rs` — add `confirm(prompt: &str) -> Result<bool>`: a fuzzel two-row `Yes`/`No` prompt. Exit-1 (no selection) → `false`; any other non-zero / signal termination → bail (mirrors the cancel-vs-failure discipline of `a0eaccc`). **Never** default to `Yes`; empty/cancel is always `No`.
- [ ] `src/verbs/quit.rs` (new) + register `quit` (System; `NIRI_SOCKET | FUZZEL`; menu-visible) → `menu::confirm("Quit jiji?")`; on `true` → `niri msg action quit --skip-confirmation`; on `false`/cancel → exit 0, dispatch nothing.
- [ ] `src/verbs/power_off_monitors.rs` (new) + register `power-off-monitors` (System; `NIRI_SOCKET | FUZZEL`) → `menu::confirm("Power off monitors?")` then `niri msg action power-off-monitors`.
- [ ] `src/cli.rs` — two new variants; parity +2.
- [ ] `tests/shims.rs` — per verb: a `Yes`-selection shim → the action argv fires; a `No`/cancel shim → **no** dispatch, exit 0. These pin the confirm gate in both directions.

### Phase 7.3 — `rename-workspace`

- [ ] `src/verbs/rename_workspace.rs` (new) + register `rename-workspace` (Workspace; `NIRI_SOCKET | FUZZEL`) → `menu::prompt_name(...)` for the new name, then `niri msg action set-workspace-name <name>` (focused ws — omit the workspace reference). Empty/cancel prompt → exit 0, no dispatch (mirror `create-activity`'s empty-prompt handling).
- [ ] `src/cli.rs` — one variant; parity +1.
- [ ] `tests/shims.rs` — prompt-shim returns a name → exact-argv pin `action set-workspace-name <name>`; empty/cancel prompt → no dispatch, exit 0.

### Phase 7.4 — `Category::Monitor` + output picker + four monitor verbs

- [ ] `src/registry.rs` — extend `Category` with `Monitor` (Ord placement after `Window`); confirm category-grouped sort.
- [ ] output picker (in `src/menu.rs` or a new `src/picker.rs` following the existing picker pattern) — read `niri msg --json outputs` at dispatch time, parse a minimal serde struct (connector name + make/model for the label), fuzzel over them, return the chosen connector. **Bail exit-1 before fuzzel** if zero outputs (NOT 69 — mirror the bail-before-picker discipline). A single output is still offered (do not auto-skip).
- [ ] `src/verbs/focus_monitor.rs` (new) + register `focus-monitor` (Monitor; `NIRI_SOCKET | FUZZEL`) → output picker → `niri msg action focus-monitor <output>`.
- [ ] `src/verbs/move_window_to_monitor.rs` (new) + register `move-window-to-monitor` (Monitor; `NIRI_SOCKET | FUZZEL`) → output picker → `niri msg action move-window-to-monitor <output>`.
- [ ] `src/verbs/move_column_to_monitor.rs` (new) + register `move-column-to-monitor` (Monitor; `NIRI_SOCKET | FUZZEL`) → output picker → `niri msg action move-column-to-monitor <output>`.
- [ ] `src/verbs/move_workspace_to_monitor.rs` (new) + register `move-workspace-to-monitor` (Monitor; `NIRI_SOCKET | FUZZEL`) → output picker → `niri msg action move-workspace-to-monitor <output>`.
- [ ] `src/cli.rs` — four variants; parity +4.
- [ ] `tests/shims.rs` — per verb: output-picker shim returns a connector → exact-argv pin; an empty-outputs run (sabotaged fuzzel) confirms the bail fires **before** fuzzel (exit 1, not 69). Exercise the shared output-picker logic once thoroughly + a thin per-verb argv pin.

### Phase 7.5 — `stop-cast` + cast picker

- [ ] cast picker (alongside the output picker) — read `niri msg --json casts` at dispatch, parse a minimal struct, fuzzel over active casts, return the session id. Bail exit-1 before fuzzel if no active casts.
- [ ] `src/verbs/stop_cast.rs` (new) + register `stop-cast` (System; `NIRI_SOCKET | FUZZEL`) → cast picker → `niri msg action stop-cast <id>`.
- [ ] `src/cli.rs` — one variant; parity +1.
- [ ] `tests/shims.rs` — cast-picker shim returns an id → argv pin; empty-casts run bails exit-1 before fuzzel.

### Phase 7.6 — `rename-activity` passthrough (depends on jiji-activities Phase 3.12)

**Blocked until** jiji-activities ships the `rename` verb (its Phase 3.12). Do not land before that.

- [ ] `src/verbs/rename_activity.rs` (new) + register `rename-activity` (Activity; `NIRI_SOCKET | FUZZEL | FORK | NIRI_ACTIVITIES`; menu-visible) → jiji-do picks the target activity (its own fuzzel picker over `niri msg --json activities`) + prompts the new name (`menu::prompt_name`), then `jiji-activities rename <new-name> --activity <target>`. Empty/cancel at either step → exit 0, no dispatch. jiji-do drives both steps so the passthrough is a single non-interactive `jiji-activities` call.
- [ ] `src/cli.rs` — one variant matching the existing activity-verb dispatch shape; parity +1.
- [ ] `tests/shims.rs` — picker + prompt shims drive a target and a name → exact `jiji-activities rename <name> --activity <target>` argv pin; cancel at the activity picker and cancel at the name prompt each → no dispatch, exit 0; empty activity inventory → bail exit-1 before the picker.

**Exit criteria (Stage 7).** All boxes `[x]`; `cargo test` green; `cargo clippy --all --all-targets` zero warnings; `cargo +nightly fmt --all` clean; the registry parity and category-grouped-ordering tests green with the two new categories; `no_forbidden_dependencies` green. Post-landing (human / chezmoi): reinstall jiji-do + bump `# hash:` once after the stage lands so the new verbs' completions regenerate.

---

## Appendix C: Deferred Suggestions

- **`src/error.rs` — `DoError::MissingCapability(String)` stringly-typed payload** — From review of `a0eaccc` (2026-05-28). Carry a typed `Capabilities` set (the unmet flags), format prose in `Display`. Type-design reviewer rated this HIGH; deferred because the machine-readable consumer (e.g. `--debug` introspection surfacing the unmet set) does not exist yet and exit-69 is already exercised by a test. Revisit in Stage 2 when `--debug` is expanded.
- **`src/snapshot.rs` — `focused_workspace` / `focused_output` joint-coupling** — From review of `a0eaccc` (2026-05-28). Nest output under a `FocusedWorkspace { id, output }` so the unrepresentable state (workspace present, output absent) is unconstructable. Reviewer: "worth fixing before more verbs read the snapshot in Stage 2."
- **`src/snapshot.rs` — `#[derive(Default)]` unused and bypasses the `from_json`/`capture` seam** — From review of `a0eaccc` (2026-05-28). Drop it so callers cannot construct an empty `Snapshot` outside the intended paths. **Promoted to Stage 6 (Phase 6.1), 2026-06-03 — tracked there, not here.**
- **`src/snapshot.rs` / `tests/shims.rs` — `parse_workspace_choices` edge-case tests missing** — From review of `a0eaccc` (2026-05-28). Null output field → `"? #id"` fallback, empty array, and malformed JSON are unexercised. (Function actually lives in `src/niri.rs`, not `src/snapshot.rs`; tests are unit tests there, not shims.) **Promoted to Stage 6 (Phase 6.1), 2026-06-03 — tracked there, not here.**
- **`src/proc.rs` / `src/capabilities.rs` — redundant `niri msg` reads between probe and capture** — From review of `a0eaccc` (2026-05-28). The probe issues `niri msg --json workspaces` / `activities` for capability detection; `Snapshot::capture` re-issues the same calls. A future stage could thread probe JSON into the snapshot to halve the subprocess count. Deferred as a perf concern — no correctness impact and the double-read is cheap relative to fuzzel startup.
- **`src/registry.rs::Verb` — conditional capability requirements based on supplied positional** — Authored 2026-05-28 alongside `create-activity`. `create-activity` requires `FUZZEL` only when no positional name is supplied; with `jiji-do create-activity foo` the direct-CLI form has no picker and could run without `FUZZEL`. The current static `requires: Capabilities` field forces the strictest set, so `jiji-do create-activity foo` errors at exit 69 when fuzzel is missing despite never spawning it. A verb-level "effective requires" computation (taking the verb_arg as input) would fix this but is over-engineering at one affected verb; revisit if a second verb with the same shape arrives.
- **`src/capabilities.rs` / `src/verbs/pick_color.rs` / `src/verbs/pick_window.rs` — `notify-send` and `wl-copy` are soft-dep subprocesses with no capability flag** — From review of `c413e97` (2026-06-03). Both tools are invoked via `proc::run_best_effort` (returns `false` on absent binary) with stdout fallback, so absence is handled gracefully at runtime. Adding `NOTIFY_SEND` / `WL_COPY` capability flags would allow the menu to surface degraded labels or skip routing steps proactively, but the one-verb-each scope makes a new flag premature. Revisit if a third verb depends on either soft dep or if a degraded-label UX is desired.

## Appendix D: Stage 2 verbs cut at Phase 2.0 ratification (restore candidates)

Verbs originally drafted for Stage 2 in `docs/launcher/initiative.md` §4 that did not survive the Phase 2.0 ratification of 2026-05-28. Recorded here so future amendments can restore any of them with one line of rationale rather than re-litigating the cut.

Cut principle applied: **exclude verbs whose action is already on a muscle-memory keybind in the standard niri config** (`~/.local/share/chezmoi/dot_config/niri/config.kdl.tmpl`). A launcher menu entry that duplicates a one-key shortcut is dead weight — the user reaches for the key, not the menu. Restoring a verb here requires either (a) a new rationale for why menu duplication adds value despite the keybind, or (b) evidence the keybind has been removed/remapped in the standard config.

| Action (kebab-case) | Category | Current keybind | Rationale for cut |
|---|---|---|---|
| `focus-workspace-up` | Workspace | `Mod+PgUp`, `Mod+K`, `Mod+WheelScrollUp` | Triple-bound; directional nav is muscle-memory. |
| `focus-workspace-down` | Workspace | `Mod+PgDn`, `Mod+J`, `Mod+WheelScrollDown` | Triple-bound; directional nav is muscle-memory. |
| `close-window` | Window | `Mod+Q` | One-key close is muscle-memory. |
| `fullscreen-window` | Window | `Mod+Shift+F` | Muscle-memory toggle. |
| `toggle-window-floating` | Window | `Mod+Semicolon` | Muscle-memory toggle. |
| `center-column` | Window | `Mod+C` | Muscle-memory. (Original initiative draft said `center-focused-column` — that's a config setting, not an action. The bound action is `center-column`.) |
| `toggle-overview` | Mode | `Mod+O` | One-key overview is muscle-memory. |
| `toggle-keyboard-shortcuts-inhibit` | Mode | `Mod+Escape` (`allow-inhibiting=false`) | Bound with the inhibit-allowed escape hatch so it works even when inhibit is active. |
| `focus-window` (fuzzel) | Window | — | Picker-based discovery verb; *not* a keybind-duplication cut. Held back because Stage 2 was rescoped to a thin discovery slice; restore alongside Stage 3 or a Stage 2.x picker batch if a clear use case emerges. |
| `close-window-by-fuzzel` | Window | — | Picker variant of `close-window`; same restore criteria as `focus-window`. |
| `move-window-to-monitor` | Window | per-direction `move-window-to-monitor-*` variants are commented out in the config (lines 548, 552); only `move-column-to-monitor-*` is bound | Conservative cut to keep Phase 2.1 scope thin. The fuzzel picker variant is restore-worthy if a multi-monitor workflow surfaces it; the per-direction action gap means it's the *only* keybind-free path today. |
| `move-column-to-monitor` | Window | per-direction variants bound to `Mod+Shift+Ctrl+<arrow>` and `Mod+Shift+Ctrl+H/L` | Directional column-to-monitor is keybind-driven; restore the fuzzel picker only if a target-list workflow surfaces. |
| `move-window-to-workspace` (fuzzel) | Window | per-direction variants bound to `Mod+Ctrl+<arrow>`/`Mod+Ctrl+PgUp/Dn`/`Mod+Ctrl+J/K`, plus numbered `Mod+Ctrl+1..9` | Picker variant; directional + numbered already cover the common cases. |
| `move-column-to-workspace` (fuzzel) | Window | same as above | Same rationale. |
| `toggle-workspace-sticky` | Workspace | — | Cut from initiative §4 in the original Phase 2.0 ratification draft (upstream-compatible duplicate of `jiji-activities`' wrapping). Restore if the upstream-fallback story for sticky workspaces becomes load-bearing.|

Note on Stage 3 (`jiji-activities` passthrough verbs): the keybind-exclusion principle does **not** apply there, since `jiji-activities` verbs have no muscle-memory direct keybinds (only spawn-jiji-activities bindings, which already go through the same passthrough path). Stage 3 keeps the full passthrough breadth.
